// Generated by Codex compiler — C++ backend
// Compile: g++ -std=c++17 -o output output.cpp
#include <cstdint>
#include <string>
#include <vector>
#include <functional>
#include <variant>
#include <iostream>
#include <cmath>
#include <algorithm>
#include <sstream>
#include <fstream>

namespace codex {


int64_t add_one(int64_t x) {
    return (x + int64_t(1));
}

int64_t _double(int64_t x) {
    return (x * int64_t(2));
}

std::string greet(std::string name) {
    return (std::string("Hello, ") + (name + std::string("!")));
}

bool is_positive(int64_t x) {
    return (x > int64_t(0));
}

int64_t apply_twice(std::function<int64_t(int64_t)> f, int64_t x) {
    return f(f(x));
}

} // namespace codex

int main() {
    using namespace codex;
    std::cout << apply_twice(add_one, int64_t(40)) << std::endl;
    return 0;
}
