// Generated by Codex compiler — C++ backend
// Compile: g++ -std=c++17 -o output output.cpp
#include <cstdint>
#include <string>
#include <vector>
#include <functional>
#include <variant>
#include <iostream>
#include <cmath>
#include <algorithm>
#include <sstream>
#include <fstream>

namespace codex {


int64_t _max(int64_t x, int64_t y) {
    return ((x > y) ? x : y);
}

int64_t _abs(int64_t x) {
    return ((x < int64_t(0)) ? (-x) : x);
}

int64_t clamp(int64_t lo, int64_t hi, int64_t x) {
    return [&]{ auto clamped = ((x < lo) ? lo : ((x > hi) ? hi : x)); return clamped; }();
}

} // namespace codex

int main() {
    using namespace codex;
    std::cout << clamp(int64_t(0), int64_t(100), _abs(_max((-int64_t(42)), int64_t(37)))) << std::endl;
    return 0;
}
