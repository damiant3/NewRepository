// Generated by Codex compiler — JavaScript backend
"use strict";

function AVariantCtorDef(name, fields) { return Object.freeze({ name: name, fields: fields }); }

function TypeDef(name, type_params, body) { return Object.freeze({ name: name, type_params: type_params, body: body }); }

function EndOfFile() { return Object.freeze({ tag: "EndOfFile" }); }
function Newline() { return Object.freeze({ tag: "Newline" }); }
function Indent() { return Object.freeze({ tag: "Indent" }); }
function Dedent() { return Object.freeze({ tag: "Dedent" }); }
function IntegerLiteral() { return Object.freeze({ tag: "IntegerLiteral" }); }
function NumberLiteral() { return Object.freeze({ tag: "NumberLiteral" }); }
function TextLiteral() { return Object.freeze({ tag: "TextLiteral" }); }
function TrueKeyword() { return Object.freeze({ tag: "TrueKeyword" }); }
function FalseKeyword() { return Object.freeze({ tag: "FalseKeyword" }); }
function Identifier() { return Object.freeze({ tag: "Identifier" }); }
function TypeIdentifier() { return Object.freeze({ tag: "TypeIdentifier" }); }
function ProseText() { return Object.freeze({ tag: "ProseText" }); }
function ChapterHeader() { return Object.freeze({ tag: "ChapterHeader" }); }
function SectionHeader() { return Object.freeze({ tag: "SectionHeader" }); }
function LetKeyword() { return Object.freeze({ tag: "LetKeyword" }); }
function InKeyword() { return Object.freeze({ tag: "InKeyword" }); }
function IfKeyword() { return Object.freeze({ tag: "IfKeyword" }); }
function ThenKeyword() { return Object.freeze({ tag: "ThenKeyword" }); }
function ElseKeyword() { return Object.freeze({ tag: "ElseKeyword" }); }
function WhenKeyword() { return Object.freeze({ tag: "WhenKeyword" }); }
function WhereKeyword() { return Object.freeze({ tag: "WhereKeyword" }); }
function SuchThatKeyword() { return Object.freeze({ tag: "SuchThatKeyword" }); }
function DoKeyword() { return Object.freeze({ tag: "DoKeyword" }); }
function RecordKeyword() { return Object.freeze({ tag: "RecordKeyword" }); }
function ImportKeyword() { return Object.freeze({ tag: "ImportKeyword" }); }
function ExportKeyword() { return Object.freeze({ tag: "ExportKeyword" }); }
function ClaimKeyword() { return Object.freeze({ tag: "ClaimKeyword" }); }
function ProofKeyword() { return Object.freeze({ tag: "ProofKeyword" }); }
function ForAllKeyword() { return Object.freeze({ tag: "ForAllKeyword" }); }
function ThereExistsKeyword() { return Object.freeze({ tag: "ThereExistsKeyword" }); }
function LinearKeyword() { return Object.freeze({ tag: "LinearKeyword" }); }
function Equals() { return Object.freeze({ tag: "Equals" }); }
function Colon() { return Object.freeze({ tag: "Colon" }); }
function Arrow() { return Object.freeze({ tag: "Arrow" }); }
function LeftArrow() { return Object.freeze({ tag: "LeftArrow" }); }
function Pipe() { return Object.freeze({ tag: "Pipe" }); }
function Ampersand() { return Object.freeze({ tag: "Ampersand" }); }
function Plus() { return Object.freeze({ tag: "Plus" }); }
function Minus() { return Object.freeze({ tag: "Minus" }); }
function Star() { return Object.freeze({ tag: "Star" }); }
function Slash() { return Object.freeze({ tag: "Slash" }); }
function Caret() { return Object.freeze({ tag: "Caret" }); }
function PlusPlus() { return Object.freeze({ tag: "PlusPlus" }); }
function ColonColon() { return Object.freeze({ tag: "ColonColon" }); }
function DoubleEquals() { return Object.freeze({ tag: "DoubleEquals" }); }
function NotEquals() { return Object.freeze({ tag: "NotEquals" }); }
function LessThan() { return Object.freeze({ tag: "LessThan" }); }
function GreaterThan() { return Object.freeze({ tag: "GreaterThan" }); }
function LessOrEqual() { return Object.freeze({ tag: "LessOrEqual" }); }
function GreaterOrEqual() { return Object.freeze({ tag: "GreaterOrEqual" }); }
function TripleEquals() { return Object.freeze({ tag: "TripleEquals" }); }
function Turnstile() { return Object.freeze({ tag: "Turnstile" }); }
function LinearProduct() { return Object.freeze({ tag: "LinearProduct" }); }
function ForAllSymbol() { return Object.freeze({ tag: "ForAllSymbol" }); }
function ExistsSymbol() { return Object.freeze({ tag: "ExistsSymbol" }); }
function LeftParen() { return Object.freeze({ tag: "LeftParen" }); }
function RightParen() { return Object.freeze({ tag: "RightParen" }); }
function LeftBracket() { return Object.freeze({ tag: "LeftBracket" }); }
function RightBracket() { return Object.freeze({ tag: "RightBracket" }); }
function LeftBrace() { return Object.freeze({ tag: "LeftBrace" }); }
function RightBrace() { return Object.freeze({ tag: "RightBrace" }); }
function Comma() { return Object.freeze({ tag: "Comma" }); }
function Dot() { return Object.freeze({ tag: "Dot" }); }
function DashGreater() { return Object.freeze({ tag: "DashGreater" }); }
function Underscore() { return Object.freeze({ tag: "Underscore" }); }
function ErrorToken() { return Object.freeze({ tag: "ErrorToken" }); }

function ExprOk(field0, field1) { return Object.freeze({ tag: "ExprOk", field0: field0, field1: field1 }); }

function IRParam(name, type_val) { return Object.freeze({ name: name, type_val: type_val }); }

function NamedType(field0) { return Object.freeze({ tag: "NamedType", field0: field0 }); }
function FunType(field0, field1) { return Object.freeze({ tag: "FunType", field0: field0, field1: field1 }); }
function AppType(field0, field1) { return Object.freeze({ tag: "AppType", field0: field0, field1: field1 }); }
function ParenType(field0) { return Object.freeze({ tag: "ParenType", field0: field0 }); }
function ListType(field0) { return Object.freeze({ tag: "ListType", field0: field0 }); }
function LinearTypeExpr(field0) { return Object.freeze({ tag: "LinearTypeExpr", field0: field0 }); }

function LetBind(name, value) { return Object.freeze({ name: name, value: value }); }

function PatOk(field0, field1) { return Object.freeze({ tag: "PatOk", field0: field0, field1: field1 }); }

function DefParamResult(state, env, remaining_type) { return Object.freeze({ state: state, env: env, remaining_type: remaining_type }); }

function SumCtor(name, fields) { return Object.freeze({ name: name, fields: fields }); }

function UnifyResult(success, state) { return Object.freeze({ success: success, state: state }); }

function TypeDefOk(field0, field1) { return Object.freeze({ tag: "TypeDefOk", field0: field0, field1: field1 }); }
function TypeDefNone(field0) { return Object.freeze({ tag: "TypeDefNone", field0: field0 }); }

function IrIntLit(field0) { return Object.freeze({ tag: "IrIntLit", field0: field0 }); }
function IrNumLit(field0) { return Object.freeze({ tag: "IrNumLit", field0: field0 }); }
function IrTextLit(field0) { return Object.freeze({ tag: "IrTextLit", field0: field0 }); }
function IrBoolLit(field0) { return Object.freeze({ tag: "IrBoolLit", field0: field0 }); }
function IrName(field0, field1) { return Object.freeze({ tag: "IrName", field0: field0, field1: field1 }); }
function IrBinary(field0, field1, field2, field3) { return Object.freeze({ tag: "IrBinary", field0: field0, field1: field1, field2: field2, field3: field3 }); }
function IrNegate(field0) { return Object.freeze({ tag: "IrNegate", field0: field0 }); }
function IrIf(field0, field1, field2, field3) { return Object.freeze({ tag: "IrIf", field0: field0, field1: field1, field2: field2, field3: field3 }); }
function IrLet(field0, field1, field2, field3) { return Object.freeze({ tag: "IrLet", field0: field0, field1: field1, field2: field2, field3: field3 }); }
function IrApply(field0, field1, field2) { return Object.freeze({ tag: "IrApply", field0: field0, field1: field1, field2: field2 }); }
function IrLambda(field0, field1, field2) { return Object.freeze({ tag: "IrLambda", field0: field0, field1: field1, field2: field2 }); }
function IrList(field0, field1) { return Object.freeze({ tag: "IrList", field0: field0, field1: field1 }); }
function IrMatch(field0, field1, field2) { return Object.freeze({ tag: "IrMatch", field0: field0, field1: field1, field2: field2 }); }
function IrDo(field0, field1) { return Object.freeze({ tag: "IrDo", field0: field0, field1: field1 }); }
function IrRecord(field0, field1, field2) { return Object.freeze({ tag: "IrRecord", field0: field0, field1: field1, field2: field2 }); }
function IrFieldAccess(field0, field1, field2) { return Object.freeze({ tag: "IrFieldAccess", field0: field0, field1: field1, field2: field2 }); }
function IrError(field0, field1) { return Object.freeze({ tag: "IrError", field0: field0, field1: field1 }); }

function AModule(name, defs, type_defs) { return Object.freeze({ name: name, defs: defs, type_defs: type_defs }); }

function ADoBindStmt(field0, field1) { return Object.freeze({ tag: "ADoBindStmt", field0: field0, field1: field1 }); }
function ADoExprStmt(field0) { return Object.freeze({ tag: "ADoExprStmt", field0: field0 }); }

function TypeBinding(name, bound_type) { return Object.freeze({ name: name, bound_type: bound_type }); }

function IrDoBind(field0, field1, field2) { return Object.freeze({ tag: "IrDoBind", field0: field0, field1: field1, field2: field2 }); }
function IrDoExec(field0) { return Object.freeze({ tag: "IrDoExec", field0: field0 }); }

function DefOk(field0, field1) { return Object.freeze({ tag: "DefOk", field0: field0, field1: field1 }); }
function DefNone(field0) { return Object.freeze({ tag: "DefNone", field0: field0 }); }

function LambdaBindResult(state, env, param_types) { return Object.freeze({ state: state, env: env, param_types: param_types }); }

function OpAdd() { return Object.freeze({ tag: "OpAdd" }); }
function OpSub() { return Object.freeze({ tag: "OpSub" }); }
function OpMul() { return Object.freeze({ tag: "OpMul" }); }
function OpDiv() { return Object.freeze({ tag: "OpDiv" }); }
function OpPow() { return Object.freeze({ tag: "OpPow" }); }
function OpEq() { return Object.freeze({ tag: "OpEq" }); }
function OpNotEq() { return Object.freeze({ tag: "OpNotEq" }); }
function OpLt() { return Object.freeze({ tag: "OpLt" }); }
function OpGt() { return Object.freeze({ tag: "OpGt" }); }
function OpLtEq() { return Object.freeze({ tag: "OpLtEq" }); }
function OpGtEq() { return Object.freeze({ tag: "OpGtEq" }); }
function OpDefEq() { return Object.freeze({ tag: "OpDefEq" }); }
function OpAppend() { return Object.freeze({ tag: "OpAppend" }); }
function OpCons() { return Object.freeze({ tag: "OpCons" }); }
function OpAnd() { return Object.freeze({ tag: "OpAnd" }); }
function OpOr() { return Object.freeze({ tag: "OpOr" }); }

function IrVarPat(field0, field1) { return Object.freeze({ tag: "IrVarPat", field0: field0, field1: field1 }); }
function IrLitPat(field0, field1) { return Object.freeze({ tag: "IrLitPat", field0: field0, field1: field1 }); }
function IrCtorPat(field0, field1, field2) { return Object.freeze({ tag: "IrCtorPat", field0: field0, field1: field1, field2: field2 }); }
function IrWildPat() { return Object.freeze({ tag: "IrWildPat" }); }

function DefSetup(expected_type, remaining_type, state, env) { return Object.freeze({ expected_type: expected_type, remaining_type: remaining_type, state: state, env: env }); }

function AMatchArm(pattern, body) { return Object.freeze({ pattern: pattern, body: body }); }

function CtorCollectResult(type_names, ctor_names) { return Object.freeze({ type_names: type_names, ctor_names: ctor_names }); }

function IrAddInt() { return Object.freeze({ tag: "IrAddInt" }); }
function IrSubInt() { return Object.freeze({ tag: "IrSubInt" }); }
function IrMulInt() { return Object.freeze({ tag: "IrMulInt" }); }
function IrDivInt() { return Object.freeze({ tag: "IrDivInt" }); }
function IrPowInt() { return Object.freeze({ tag: "IrPowInt" }); }
function IrAddNum() { return Object.freeze({ tag: "IrAddNum" }); }
function IrSubNum() { return Object.freeze({ tag: "IrSubNum" }); }
function IrMulNum() { return Object.freeze({ tag: "IrMulNum" }); }
function IrDivNum() { return Object.freeze({ tag: "IrDivNum" }); }
function IrEq() { return Object.freeze({ tag: "IrEq" }); }
function IrNotEq() { return Object.freeze({ tag: "IrNotEq" }); }
function IrLt() { return Object.freeze({ tag: "IrLt" }); }
function IrGt() { return Object.freeze({ tag: "IrGt" }); }
function IrLtEq() { return Object.freeze({ tag: "IrLtEq" }); }
function IrGtEq() { return Object.freeze({ tag: "IrGtEq" }); }
function IrAnd() { return Object.freeze({ tag: "IrAnd" }); }
function IrOr() { return Object.freeze({ tag: "IrOr" }); }
function IrAppendText() { return Object.freeze({ tag: "IrAppendText" }); }
function IrAppendList() { return Object.freeze({ tag: "IrAppendList" }); }
function IrConsList() { return Object.freeze({ tag: "IrConsList" }); }

function RecordField(name, type_val) { return Object.freeze({ name: name, type_val: type_val }); }

function LitExpr(field0) { return Object.freeze({ tag: "LitExpr", field0: field0 }); }
function NameExpr(field0) { return Object.freeze({ tag: "NameExpr", field0: field0 }); }
function AppExpr(field0, field1) { return Object.freeze({ tag: "AppExpr", field0: field0, field1: field1 }); }
function BinExpr(field0, field1, field2) { return Object.freeze({ tag: "BinExpr", field0: field0, field1: field1, field2: field2 }); }
function UnaryExpr(field0, field1) { return Object.freeze({ tag: "UnaryExpr", field0: field0, field1: field1 }); }
function IfExpr(field0, field1, field2) { return Object.freeze({ tag: "IfExpr", field0: field0, field1: field1, field2: field2 }); }
function LetExpr(field0, field1) { return Object.freeze({ tag: "LetExpr", field0: field0, field1: field1 }); }
function MatchExpr(field0, field1) { return Object.freeze({ tag: "MatchExpr", field0: field0, field1: field1 }); }
function ListExpr(field0) { return Object.freeze({ tag: "ListExpr", field0: field0 }); }
function RecordExpr(field0, field1) { return Object.freeze({ tag: "RecordExpr", field0: field0, field1: field1 }); }
function FieldExpr(field0, field1) { return Object.freeze({ tag: "FieldExpr", field0: field0, field1: field1 }); }
function ParenExpr(field0) { return Object.freeze({ tag: "ParenExpr", field0: field0 }); }
function DoExpr(field0) { return Object.freeze({ tag: "DoExpr", field0: field0 }); }
function ErrExpr(field0) { return Object.freeze({ tag: "ErrExpr", field0: field0 }); }

function RecordBody(field0) { return Object.freeze({ tag: "RecordBody", field0: field0 }); }
function VariantBody(field0) { return Object.freeze({ tag: "VariantBody", field0: field0 }); }

function ModuleResult(types, state) { return Object.freeze({ types: types, state: state }); }

function Document(defs, type_defs) { return Object.freeze({ defs: defs, type_defs: type_defs }); }

function Token(kind, text, offset, line, column) { return Object.freeze({ kind: kind, text: text, offset: offset, line: line, column: column }); }

function Name(value) { return Object.freeze({ value: value }); }

function TypeOk(field0, field1) { return Object.freeze({ tag: "TypeOk", field0: field0, field1: field1 }); }

function ARecordFieldDef(name, type_expr) { return Object.freeze({ name: name, type_expr: type_expr }); }

function SourceSpan(start, end, file) { return Object.freeze({ start: start, end: end, file: file }); }

function LetBindResult(state, env) { return Object.freeze({ state: state, env: env }); }

function IntLit() { return Object.freeze({ tag: "IntLit" }); }
function NumLit() { return Object.freeze({ tag: "NumLit" }); }
function TextLit() { return Object.freeze({ tag: "TextLit" }); }
function BoolLit() { return Object.freeze({ tag: "BoolLit" }); }

function IntegerTy() { return Object.freeze({ tag: "IntegerTy" }); }
function NumberTy() { return Object.freeze({ tag: "NumberTy" }); }
function TextTy() { return Object.freeze({ tag: "TextTy" }); }
function BooleanTy() { return Object.freeze({ tag: "BooleanTy" }); }
function VoidTy() { return Object.freeze({ tag: "VoidTy" }); }
function NothingTy() { return Object.freeze({ tag: "NothingTy" }); }
function ErrorTy() { return Object.freeze({ tag: "ErrorTy" }); }
function FunTy(field0, field1) { return Object.freeze({ tag: "FunTy", field0: field0, field1: field1 }); }
function ListTy(field0) { return Object.freeze({ tag: "ListTy", field0: field0 }); }
function TypeVar(field0) { return Object.freeze({ tag: "TypeVar", field0: field0 }); }
function ForAllTy(field0, field1) { return Object.freeze({ tag: "ForAllTy", field0: field0, field1: field1 }); }
function SumTy(field0, field1) { return Object.freeze({ tag: "SumTy", field0: field0, field1: field1 }); }
function RecordTy(field0, field1) { return Object.freeze({ tag: "RecordTy", field0: field0, field1: field1 }); }
function ConstructedTy(field0, field1) { return Object.freeze({ tag: "ConstructedTy", field0: field0, field1: field1 }); }

function ADef(name, params, declared_type, body) { return Object.freeze({ name: name, params: params, declared_type: declared_type, body: body }); }

function UnificationState(substitutions, next_id, errors) { return Object.freeze({ substitutions: substitutions, next_id: next_id, errors: errors }); }

function IRBranch(pattern, body) { return Object.freeze({ pattern: pattern, body: body }); }

function Def(name, params, ann, body) { return Object.freeze({ name: name, params: params, ann: ann, body: body }); }

function IRDef(name, params, type_val, body) { return Object.freeze({ name: name, params: params, type_val: type_val, body: body }); }

function TypeEnv(bindings) { return Object.freeze({ bindings: bindings }); }

function SubstEntry(var_id, resolved_type) { return Object.freeze({ var_id: var_id, resolved_type: resolved_type }); }

function RecordFieldDef(name, type_expr) { return Object.freeze({ name: name, type_expr: type_expr }); }

function ALetBind(name, value) { return Object.freeze({ name: name, value: value }); }

function VariantCtorDef(name, fields) { return Object.freeze({ name: name, fields: fields }); }

function VarPat(field0) { return Object.freeze({ tag: "VarPat", field0: field0 }); }
function LitPat(field0) { return Object.freeze({ tag: "LitPat", field0: field0 }); }
function CtorPat(field0, field1) { return Object.freeze({ tag: "CtorPat", field0: field0, field1: field1 }); }
function WildPat(field0) { return Object.freeze({ tag: "WildPat", field0: field0 }); }

function FreshResult(var_type, state) { return Object.freeze({ var_type: var_type, state: state }); }

function ResolveResult(errors, top_level_names, type_names, ctor_names) { return Object.freeze({ errors: errors, top_level_names: top_level_names, type_names: type_names, ctor_names: ctor_names }); }

function ALitExpr(field0, field1) { return Object.freeze({ tag: "ALitExpr", field0: field0, field1: field1 }); }
function ANameExpr(field0) { return Object.freeze({ tag: "ANameExpr", field0: field0 }); }
function AApplyExpr(field0, field1) { return Object.freeze({ tag: "AApplyExpr", field0: field0, field1: field1 }); }
function ABinaryExpr(field0, field1, field2) { return Object.freeze({ tag: "ABinaryExpr", field0: field0, field1: field1, field2: field2 }); }
function AUnaryExpr(field0) { return Object.freeze({ tag: "AUnaryExpr", field0: field0 }); }
function AIfExpr(field0, field1, field2) { return Object.freeze({ tag: "AIfExpr", field0: field0, field1: field1, field2: field2 }); }
function ALetExpr(field0, field1) { return Object.freeze({ tag: "ALetExpr", field0: field0, field1: field1 }); }
function ALambdaExpr(field0, field1) { return Object.freeze({ tag: "ALambdaExpr", field0: field0, field1: field1 }); }
function AMatchExpr(field0, field1) { return Object.freeze({ tag: "AMatchExpr", field0: field0, field1: field1 }); }
function AListExpr(field0) { return Object.freeze({ tag: "AListExpr", field0: field0 }); }
function ARecordExpr(field0, field1) { return Object.freeze({ tag: "ARecordExpr", field0: field0, field1: field1 }); }
function AFieldAccess(field0, field1) { return Object.freeze({ tag: "AFieldAccess", field0: field0, field1: field1 }); }
function ADoExpr(field0) { return Object.freeze({ tag: "ADoExpr", field0: field0 }); }
function AErrorExpr(field0) { return Object.freeze({ tag: "AErrorExpr", field0: field0 }); }

function Scope(names) { return Object.freeze({ names: names }); }

function SourcePosition(line, column, offset) { return Object.freeze({ line: line, column: column, offset: offset }); }

function CompileOk(field0, field1) { return Object.freeze({ tag: "CompileOk", field0: field0, field1: field1 }); }
function CompileError(field0) { return Object.freeze({ tag: "CompileError", field0: field0 }); }

function RecordFieldExpr(name, value) { return Object.freeze({ name: name, value: value }); }

function AFieldExpr(name, value) { return Object.freeze({ name: name, value: value }); }

function TypeAnn(name, type_expr) { return Object.freeze({ name: name, type_expr: type_expr }); }

function MatchArm(pattern, body) { return Object.freeze({ pattern: pattern, body: body }); }

function ANamedType(field0) { return Object.freeze({ tag: "ANamedType", field0: field0 }); }
function AFunType(field0, field1) { return Object.freeze({ tag: "AFunType", field0: field0, field1: field1 }); }
function AAppType(field0, field1) { return Object.freeze({ tag: "AAppType", field0: field0, field1: field1 }); }

function AVarPat(field0) { return Object.freeze({ tag: "AVarPat", field0: field0 }); }
function ALitPat(field0, field1) { return Object.freeze({ tag: "ALitPat", field0: field0, field1: field1 }); }
function ACtorPat(field0, field1) { return Object.freeze({ tag: "ACtorPat", field0: field0, field1: field1 }); }
function AWildPat() { return Object.freeze({ tag: "AWildPat" }); }

function IRFieldVal(name, value) { return Object.freeze({ name: name, value: value }); }

function AParam(name) { return Object.freeze({ name: name }); }

function CollectResult(names, errors) { return Object.freeze({ names: names, errors: errors }); }

function IRModule(name, defs) { return Object.freeze({ name: name, defs: defs }); }

function ARecordTypeDef(field0, field1, field2) { return Object.freeze({ tag: "ARecordTypeDef", field0: field0, field1: field1, field2: field2 }); }
function AVariantTypeDef(field0, field1, field2) { return Object.freeze({ tag: "AVariantTypeDef", field0: field0, field1: field1, field2: field2 }); }

function Diagnostic(code, message, severity) { return Object.freeze({ code: code, message: message, severity: severity }); }

function Error() { return Object.freeze({ tag: "Error" }); }
function Warning() { return Object.freeze({ tag: "Warning" }); }
function Info() { return Object.freeze({ tag: "Info" }); }

function LexToken(field0, field1) { return Object.freeze({ tag: "LexToken", field0: field0, field1: field1 }); }
function LexEnd() { return Object.freeze({ tag: "LexEnd" }); }

function ParseState(tokens, pos) { return Object.freeze({ tokens: tokens, pos: pos }); }

function CheckResult(inferred_type, state) { return Object.freeze({ inferred_type: inferred_type, state: state }); }

function DoBindStmt(field0, field1) { return Object.freeze({ tag: "DoBindStmt", field0: field0, field1: field1 }); }
function DoExprStmt(field0) { return Object.freeze({ tag: "DoExprStmt", field0: field0 }); }

function LexState(source, offset, line, column) { return Object.freeze({ source: source, offset: offset, line: line, column: column }); }

function desugar_expr(node) {
    while (true) {
        return ((_s) => (_s.tag === "LitExpr" ? (() => { const tok = _s.field0; return desugar_literal(tok); })() : (_s.tag === "NameExpr" ? (() => { const tok = _s.field0; return ANameExpr(make_name(tok.text)); })() : (_s.tag === "AppExpr" ? (() => { const f = _s.field0; const a = _s.field1; return AApplyExpr(desugar_expr(f), desugar_expr(a)); })() : (_s.tag === "BinExpr" ? (() => { const l = _s.field0; const op = _s.field1; const r = _s.field2; return ABinaryExpr(desugar_expr(l), desugar_bin_op(op.kind), desugar_expr(r)); })() : (_s.tag === "UnaryExpr" ? (() => { const op = _s.field0; const operand = _s.field1; return AUnaryExpr(desugar_expr(operand)); })() : (_s.tag === "IfExpr" ? (() => { const c = _s.field0; const t = _s.field1; const e = _s.field2; return AIfExpr(desugar_expr(c), desugar_expr(t), desugar_expr(e)); })() : (_s.tag === "LetExpr" ? (() => { const bindings = _s.field0; const body = _s.field1; return ALetExpr(map_list(desugar_let_bind, bindings), desugar_expr(body)); })() : (_s.tag === "MatchExpr" ? (() => { const scrut = _s.field0; const arms = _s.field1; return AMatchExpr(desugar_expr(scrut), map_list(desugar_match_arm, arms)); })() : (_s.tag === "ListExpr" ? (() => { const elems = _s.field0; return AListExpr(map_list(desugar_expr, elems)); })() : (_s.tag === "RecordExpr" ? (() => { const type_tok = _s.field0; const fields = _s.field1; return ARecordExpr(make_name(type_tok.text), map_list(desugar_field_expr, fields)); })() : (_s.tag === "FieldExpr" ? (() => { const rec = _s.field0; const field_tok = _s.field1; return AFieldAccess(desugar_expr(rec), make_name(field_tok.text)); })() : (_s.tag === "ParenExpr" ? (() => { const inner = _s.field0; return desugar_expr(inner); })() : (_s.tag === "DoExpr" ? (() => { const stmts = _s.field0; return ADoExpr(map_list(desugar_do_stmt, stmts)); })() : (_s.tag === "ErrExpr" ? (() => { const tok = _s.field0; return AErrorExpr(tok.text); })() : (() => { throw new Error("Non-exhaustive match"); })())))))))))))))))(node);
    }
}

function desugar_literal(tok) {
    return (is_literal(tok.kind) ? ALitExpr(tok.text, classify_literal(tok.kind)) : AErrorExpr(tok.text));
}

function classify_literal(k) {
    return ((_s) => (_s.tag === "IntegerLiteral" ? IntLit() : (_s.tag === "NumberLiteral" ? NumLit() : (_s.tag === "TextLiteral" ? TextLit() : (_s.tag === "TrueKeyword" ? BoolLit() : (_s.tag === "FalseKeyword" ? BoolLit() : ((_) => TextLit())(_s)))))))(k);
}

function desugar_let_bind(b) {
    return ALetBind(make_name(b.name.text), desugar_expr(b.value));
}

function desugar_match_arm(arm) {
    return AMatchArm(desugar_pattern(arm.pattern), desugar_expr(arm.body));
}

function desugar_field_expr(f) {
    return AFieldExpr(make_name(f.name.text), desugar_expr(f.value));
}

function desugar_do_stmt(s) {
    return ((_s) => (_s.tag === "DoBindStmt" ? (() => { const tok = _s.field0; const val = _s.field1; return ADoBindStmt(make_name(tok.text), desugar_expr(val)); })() : (_s.tag === "DoExprStmt" ? (() => { const e = _s.field0; return ADoExprStmt(desugar_expr(e)); })() : (() => { throw new Error("Non-exhaustive match"); })())))(s);
}

function desugar_bin_op(k) {
    return ((_s) => (_s.tag === "Plus" ? OpAdd() : (_s.tag === "Minus" ? OpSub() : (_s.tag === "Star" ? OpMul() : (_s.tag === "Slash" ? OpDiv() : (_s.tag === "Caret" ? OpPow() : (_s.tag === "DoubleEquals" ? OpEq() : (_s.tag === "NotEquals" ? OpNotEq() : (_s.tag === "LessThan" ? OpLt() : (_s.tag === "GreaterThan" ? OpGt() : (_s.tag === "LessOrEqual" ? OpLtEq() : (_s.tag === "GreaterOrEqual" ? OpGtEq() : (_s.tag === "TripleEquals" ? OpDefEq() : (_s.tag === "PlusPlus" ? OpAppend() : (_s.tag === "ColonColon" ? OpCons() : (_s.tag === "Ampersand" ? OpAnd() : (_s.tag === "Pipe" ? OpOr() : ((_) => OpAdd())(_s))))))))))))))))))(k);
}

function desugar_pattern(p) {
    return ((_s) => (_s.tag === "VarPat" ? (() => { const tok = _s.field0; return AVarPat(make_name(tok.text)); })() : (_s.tag === "LitPat" ? (() => { const tok = _s.field0; return ALitPat(tok.text, classify_literal(tok.kind)); })() : (_s.tag === "CtorPat" ? (() => { const tok = _s.field0; const subs = _s.field1; return ACtorPat(make_name(tok.text), map_list(desugar_pattern, subs)); })() : (_s.tag === "WildPat" ? (() => { const tok = _s.field0; return AWildPat(); })() : (() => { throw new Error("Non-exhaustive match"); })())))))(p);
}

function desugar_type_expr(t) {
    while (true) {
        return ((_s) => (_s.tag === "NamedType" ? (() => { const tok = _s.field0; return ANamedType(make_name(tok.text)); })() : (_s.tag === "FunType" ? (() => { const param = _s.field0; const ret = _s.field1; return AFunType(desugar_type_expr(param), desugar_type_expr(ret)); })() : (_s.tag === "AppType" ? (() => { const ctor = _s.field0; const args = _s.field1; return AAppType(desugar_type_expr(ctor), map_list(desugar_type_expr, args)); })() : (_s.tag === "ParenType" ? (() => { const inner = _s.field0; return desugar_type_expr(inner); })() : (_s.tag === "ListType" ? (() => { const elem = _s.field0; return AAppType(ANamedType(make_name("List")), [desugar_type_expr(elem)]); })() : (_s.tag === "LinearTypeExpr" ? (() => { const inner = _s.field0; return desugar_type_expr(inner); })() : (() => { throw new Error("Non-exhaustive match"); })())))))))(t);
    }
}

function desugar_def(d) {
    return ADef(make_name(d.name.text), map_list(desugar_param, d.params), [], desugar_expr(d.body));
}

function desugar_param(tok) {
    return AParam(make_name(tok.text));
}

function desugar_type_def(td) {
    return ((_s) => (_s.tag === "RecordBody" ? (() => { const fields = _s.field0; return ARecordTypeDef(make_name(td.name.text), map_list(make_type_param_name, td.type_params), map_list(desugar_record_field_def, fields)); })() : (_s.tag === "VariantBody" ? (() => { const ctors = _s.field0; return AVariantTypeDef(make_name(td.name.text), map_list(make_type_param_name, td.type_params), map_list(desugar_variant_ctor_def, ctors)); })() : (() => { throw new Error("Non-exhaustive match"); })())))(td.body);
}

function make_type_param_name(tok) {
    return make_name(tok.text);
}

function desugar_record_field_def(f) {
    return ARecordFieldDef(make_name(f.name.text), desugar_type_expr(f.type_expr));
}

function desugar_variant_ctor_def(c) {
    return AVariantCtorDef(make_name(c.name.text), map_list(desugar_type_expr, c.fields));
}

function desugar_document(doc, module_name) {
    return AModule(make_name(module_name), map_list(desugar_def, doc.defs), map_list(desugar_type_def, doc.type_defs));
}

function map_list(f, xs) {
    return map_list_loop(f, xs, 0n, BigInt(xs.length), []);
}

function map_list_loop(f, xs, i, len, acc) {
    while (true) {
        if ((i === len)) {
            return acc;
        } else {
            const _tco_0 = f;
            const _tco_1 = xs;
            const _tco_2 = (i + 1n);
            const _tco_3 = len;
            const _tco_4 = [...acc, ...[f(xs[Number(i)])]];
            f = _tco_0;
            xs = _tco_1;
            i = _tco_2;
            len = _tco_3;
            acc = _tco_4;
            continue;
        }
    }
}

function fold_list(f, z, xs) {
    return fold_list_loop(f, z, xs, 0n, BigInt(xs.length));
}

function fold_list_loop(f, z, xs, i, len) {
    while (true) {
        if ((i === len)) {
            return z;
        } else {
            const _tco_0 = f;
            const _tco_1 = f(z)(xs[Number(i)]);
            const _tco_2 = xs;
            const _tco_3 = (i + 1n);
            const _tco_4 = len;
            f = _tco_0;
            z = _tco_1;
            xs = _tco_2;
            i = _tco_3;
            len = _tco_4;
            continue;
        }
    }
}

function make_error(code, msg) {
    return Diagnostic(code, msg, Error());
}

function make_warning(code, msg) {
    return Diagnostic(code, msg, Warning());
}

function severity_label(s) {
    return ((_s) => (_s.tag === "Error" ? "error" : (_s.tag === "Warning" ? "warning" : (_s.tag === "Info" ? "info" : (() => { throw new Error("Non-exhaustive match"); })()))))(s);
}

function diagnostic_display(d) {
    return (severity_label(d.severity) + (" " + (d.code + (": " + d.message))));
}

function make_name(s) {
    return Name(s);
}

function name_value(n) {
    return n.value;
}

function make_position(line, col, offset) {
    return SourcePosition(line, col, offset);
}

function make_span(s, e, f) {
    return SourceSpan(s, e, f);
}

function span_length(span) {
    return (span.end.offset - span.start.offset);
}

function sanitize(name) {
    return name.replaceAll("-", "_");
}

function cs_type(ty) {
    while (true) {
        return ((_s) => (_s.tag === "IntegerTy" ? "long" : (_s.tag === "NumberTy" ? "decimal" : (_s.tag === "TextTy" ? "string" : (_s.tag === "BooleanTy" ? "bool" : (_s.tag === "VoidTy" ? "void" : (_s.tag === "NothingTy" ? "object" : (_s.tag === "ErrorTy" ? "object" : (_s.tag === "FunTy" ? (() => { const p = _s.field0; const r = _s.field1; return ("Func<" + (cs_type(p) + (", " + (cs_type(r) + ">")))); })() : (_s.tag === "ListTy" ? (() => { const elem = _s.field0; return ("List<" + (cs_type(elem) + ">")); })() : (_s.tag === "TypeVar" ? (() => { const id = _s.field0; return "object"; })() : (_s.tag === "ForAllTy" ? (() => { const id = _s.field0; const body = _s.field1; return cs_type(body); })() : (_s.tag === "SumTy" ? (() => { const name = _s.field0; const ctors = _s.field1; return sanitize(name.value); })() : (_s.tag === "RecordTy" ? (() => { const name = _s.field0; const fields = _s.field1; return sanitize(name.value); })() : (_s.tag === "ConstructedTy" ? (() => { const name = _s.field0; const args = _s.field1; return sanitize(name.value); })() : (() => { throw new Error("Non-exhaustive match"); })())))))))))))))))(ty);
    }
}

function emit_expr(e) {
    return ((_s) => (_s.tag === "IrIntLit" ? (() => { const n = _s.field0; return String(n); })() : (_s.tag === "IrNumLit" ? (() => { const n = _s.field0; return String(n); })() : (_s.tag === "IrTextLit" ? (() => { const s = _s.field0; return ("\"" + (escape_text(s) + "\"")); })() : (_s.tag === "IrBoolLit" ? (() => { const b = _s.field0; return (b ? "true" : "false"); })() : (_s.tag === "IrName" ? (() => { const n = _s.field0; const ty = _s.field1; return sanitize(n); })() : (_s.tag === "IrBinary" ? (() => { const op = _s.field0; const l = _s.field1; const r = _s.field2; const ty = _s.field3; return ("(" + (emit_expr(l) + (" " + (emit_bin_op(op) + (" " + (emit_expr(r) + ")")))))); })() : (_s.tag === "IrNegate" ? (() => { const operand = _s.field0; return ("(-" + (emit_expr(operand) + ")")); })() : (_s.tag === "IrIf" ? (() => { const c = _s.field0; const t = _s.field1; const el = _s.field2; const ty = _s.field3; return ("(" + (emit_expr(c) + (" ? " + (emit_expr(t) + (" : " + (emit_expr(el) + ")")))))); })() : (_s.tag === "IrLet" ? (() => { const name = _s.field0; const ty = _s.field1; const val = _s.field2; const body = _s.field3; return emit_let(name, ty, val, body); })() : (_s.tag === "IrApply" ? (() => { const f = _s.field0; const a = _s.field1; const ty = _s.field2; return (emit_expr(f) + ("(" + (emit_expr(a) + ")"))); })() : (_s.tag === "IrLambda" ? (() => { const params = _s.field0; const body = _s.field1; const ty = _s.field2; return emit_lambda(params, body); })() : (_s.tag === "IrList" ? (() => { const elems = _s.field0; const ty = _s.field1; return emit_list(elems, ty); })() : (_s.tag === "IrMatch" ? (() => { const scrut = _s.field0; const branches = _s.field1; const ty = _s.field2; return emit_match(scrut, branches, ty); })() : (_s.tag === "IrDo" ? (() => { const stmts = _s.field0; const ty = _s.field1; return emit_do(stmts); })() : (_s.tag === "IrRecord" ? (() => { const name = _s.field0; const fields = _s.field1; const ty = _s.field2; return emit_record(name, fields); })() : (_s.tag === "IrFieldAccess" ? (() => { const rec = _s.field0; const field = _s.field1; const ty = _s.field2; return (emit_expr(rec) + ("." + sanitize(field))); })() : (_s.tag === "IrError" ? (() => { const msg = _s.field0; const ty = _s.field1; return ("/* error: " + (msg + " */ default")); })() : (() => { throw new Error("Non-exhaustive match"); })()))))))))))))))))))(e);
}

function escape_text(s) {
    return s.replaceAll("\\", "\\\\").replaceAll("\"", "\\\"");
}

function emit_bin_op(op) {
    return ((_s) => (_s.tag === "IrAddInt" ? "+" : (_s.tag === "IrSubInt" ? "-" : (_s.tag === "IrMulInt" ? "*" : (_s.tag === "IrDivInt" ? "/" : (_s.tag === "IrPowInt" ? "^" : (_s.tag === "IrAddNum" ? "+" : (_s.tag === "IrSubNum" ? "-" : (_s.tag === "IrMulNum" ? "*" : (_s.tag === "IrDivNum" ? "/" : (_s.tag === "IrEq" ? "==" : (_s.tag === "IrNotEq" ? "!=" : (_s.tag === "IrLt" ? "<" : (_s.tag === "IrGt" ? ">" : (_s.tag === "IrLtEq" ? "<=" : (_s.tag === "IrGtEq" ? ">=" : (_s.tag === "IrAnd" ? "&&" : (_s.tag === "IrOr" ? "||" : (_s.tag === "IrAppendText" ? "+" : (_s.tag === "IrAppendList" ? "+" : (_s.tag === "IrConsList" ? "+" : (() => { throw new Error("Non-exhaustive match"); })())))))))))))))))))))))(op);
}

function emit_let(name, ty, val, body) {
    return ("((" + (cs_type(ty) + (" " + (sanitize(name) + (" = " + (emit_expr(val) + (") is var _ ? " + (emit_expr(body) + " : default)"))))))));
}

function emit_lambda(params, body) {
    return ((BigInt(params.length) === 0n) ? ("(() => " + (emit_expr(body) + ")")) : ((BigInt(params.length) === 1n) ? ((p) => ("((" + (cs_type(p.type_val) + (" " + (sanitize(p.name) + (") => " + (emit_expr(body) + ")")))))))(params[Number(0n)]) : ("(() => " + (emit_expr(body) + ")"))));
}

function emit_list(elems, ty) {
    return ((BigInt(elems.length) === 0n) ? ("new List<" + (cs_type(ty) + ">()")) : ("new List<" + (cs_type(ty) + ("> { " + (emit_list_elems(elems, 0n) + " }")))));
}

function emit_list_elems(elems, i) {
    return ((i === BigInt(elems.length)) ? "" : ((i === (BigInt(elems.length) - 1n)) ? emit_expr(elems[Number(i)]) : (emit_expr(elems[Number(i)]) + (", " + emit_list_elems(elems, (i + 1n))))));
}

function emit_match(scrut, branches, ty) {
    return (emit_expr(scrut) + (" switch { " + (emit_match_arms(branches, 0n) + " }")));
}

function emit_match_arms(branches, i) {
    return ((i === BigInt(branches.length)) ? "" : ((arm) => (emit_pattern(arm.pattern) + (" => " + (emit_expr(arm.body) + (", " + emit_match_arms(branches, (i + 1n)))))))(branches[Number(i)]));
}

function emit_pattern(p) {
    return ((_s) => (_s.tag === "IrVarPat" ? (() => { const name = _s.field0; const ty = _s.field1; return (cs_type(ty) + (" " + sanitize(name))); })() : (_s.tag === "IrLitPat" ? (() => { const text = _s.field0; const ty = _s.field1; return text; })() : (_s.tag === "IrCtorPat" ? (() => { const name = _s.field0; const subs = _s.field1; const ty = _s.field2; return ((BigInt(subs.length) === 0n) ? (sanitize(name) + " { }") : (sanitize(name) + ("(" + (emit_sub_patterns(subs, 0n) + ")")))); })() : (_s.tag === "IrWildPat" ? "_" : (() => { throw new Error("Non-exhaustive match"); })())))))(p);
}

function emit_sub_patterns(subs, i) {
    return ((i === BigInt(subs.length)) ? "" : ((sub) => (emit_sub_pattern(sub) + (((i < (BigInt(subs.length) - 1n)) ? ", " : "") + emit_sub_patterns(subs, (i + 1n)))))(subs[Number(i)]));
}

function emit_sub_pattern(p) {
    return ((_s) => (_s.tag === "IrVarPat" ? (() => { const name = _s.field0; const ty = _s.field1; return ("var " + sanitize(name)); })() : (_s.tag === "IrCtorPat" ? (() => { const name = _s.field0; const subs = _s.field1; const ty = _s.field2; return emit_pattern(p); })() : (_s.tag === "IrWildPat" ? "_" : (_s.tag === "IrLitPat" ? (() => { const text = _s.field0; const ty = _s.field1; return text; })() : (() => { throw new Error("Non-exhaustive match"); })())))))(p);
}

function emit_do(stmts) {
    return ("{ " + (emit_do_stmts(stmts, 0n) + " }"));
}

function emit_do_stmts(stmts, i) {
    return ((i === BigInt(stmts.length)) ? "" : ((s) => (emit_do_stmt(s) + (" " + emit_do_stmts(stmts, (i + 1n)))))(stmts[Number(i)]));
}

function emit_do_stmt(s) {
    return ((_s) => (_s.tag === "IrDoBind" ? (() => { const name = _s.field0; const ty = _s.field1; const val = _s.field2; return ("var " + (sanitize(name) + (" = " + (emit_expr(val) + ";")))); })() : (_s.tag === "IrDoExec" ? (() => { const e = _s.field0; return (emit_expr(e) + ";"); })() : (() => { throw new Error("Non-exhaustive match"); })())))(s);
}

function emit_record(name, fields) {
    return ("new " + (sanitize(name) + ("(" + (emit_record_fields(fields, 0n) + ")"))));
}

function emit_record_fields(fields, i) {
    return ((i === BigInt(fields.length)) ? "" : ((f) => (sanitize(f.name) + (": " + (emit_expr(f.value) + (((i < (BigInt(fields.length) - 1n)) ? ", " : "") + emit_record_fields(fields, (i + 1n)))))))(fields[Number(i)]));
}

function emit_type_defs(tds, i) {
    return ((i === BigInt(tds.length)) ? "" : (emit_type_def(tds[Number(i)]) + ("\n" + emit_type_defs(tds, (i + 1n)))));
}

function emit_type_def(td) {
    return ((_s) => (_s.tag === "ARecordTypeDef" ? (() => { const name = _s.field0; const tparams = _s.field1; const fields = _s.field2; return ("public sealed record " + (sanitize(name.value) + ("(" + (emit_record_field_defs(fields, 0n) + ");\n")))); })() : (_s.tag === "AVariantTypeDef" ? (() => { const name = _s.field0; const tparams = _s.field1; const ctors = _s.field2; return ("public abstract record " + (sanitize(name.value) + (";\n" + (emit_variant_ctors(ctors, name, 0n) + "\n")))); })() : (() => { throw new Error("Non-exhaustive match"); })())))(td);
}

function emit_record_field_defs(fields, i) {
    return ((i === BigInt(fields.length)) ? "" : ((f) => (emit_type_expr(f.type_expr) + (" " + (sanitize(f.name.value) + (((i < (BigInt(fields.length) - 1n)) ? ", " : "") + emit_record_field_defs(fields, (i + 1n)))))))(fields[Number(i)]));
}

function emit_variant_ctors(ctors, base_name, i) {
    return ((i === BigInt(ctors.length)) ? "" : ((c) => (emit_variant_ctor(c, base_name) + emit_variant_ctors(ctors, base_name, (i + 1n))))(ctors[Number(i)]));
}

function emit_variant_ctor(c, base_name) {
    return ((BigInt(c.fields.length) === 0n) ? ("public sealed record " + (sanitize(c.name.value) + (" : " + (sanitize(base_name.value) + ";\n")))) : ("public sealed record " + (sanitize(c.name.value) + ("(" + (emit_ctor_fields(c.fields, 0n) + (") : " + (sanitize(base_name.value) + ";\n")))))));
}

function emit_ctor_fields(fields, i) {
    return ((i === BigInt(fields.length)) ? "" : (emit_type_expr(fields[Number(i)]) + (" Field" + (String(i) + (((i < (BigInt(fields.length) - 1n)) ? ", " : "") + emit_ctor_fields(fields, (i + 1n)))))));
}

function emit_type_expr(te) {
    return ((_s) => (_s.tag === "ANamedType" ? (() => { const name = _s.field0; return when_type_name(name.value); })() : (_s.tag === "AFunType" ? (() => { const p = _s.field0; const r = _s.field1; return ("Func<" + (emit_type_expr(p) + (", " + (emit_type_expr(r) + ">")))); })() : (_s.tag === "AAppType" ? (() => { const base = _s.field0; const args = _s.field1; return (emit_type_expr(base) + ("<" + (emit_type_expr_list(args, 0n) + ">"))); })() : (() => { throw new Error("Non-exhaustive match"); })()))))(te);
}

function when_type_name(n) {
    return ((n === "Integer") ? "long" : ((n === "Number") ? "decimal" : ((n === "Text") ? "string" : ((n === "Boolean") ? "bool" : ((n === "List") ? "List" : sanitize(n))))));
}

function emit_type_expr_list(args, i) {
    return ((i === BigInt(args.length)) ? "" : (emit_type_expr(args[Number(i)]) + (((i < (BigInt(args.length) - 1n)) ? ", " : "") + emit_type_expr_list(args, (i + 1n)))));
}

function emit_def(d) {
    return ("    public static " + (cs_type(d.type_val) + (" " + (sanitize(d.name) + ("(" + (emit_def_params(d.params, 0n) + (") => " + (emit_expr(d.body) + ";\n"))))))));
}

function emit_def_params(params, i) {
    return ((i === BigInt(params.length)) ? "" : ((p) => (cs_type(p.type_val) + (" " + (sanitize(p.name) + (((i < (BigInt(params.length) - 1n)) ? ", " : "") + emit_def_params(params, (i + 1n)))))))(params[Number(i)]));
}

function emit_full_module(m, type_defs) {
    return ("using System;\nusing System.Collections.Generic;\nusing System.Linq;\n\n" + (emit_type_defs(type_defs, 0n) + (emit_class_header(m.name.value) + (emit_defs(m.defs, 0n) + "}\n"))));
}

function emit_module(m) {
    return ("using System;\nusing System.Collections.Generic;\nusing System.Linq;\n\n" + (emit_class_header(m.name.value) + (emit_defs(m.defs, 0n) + "}\n")));
}

function emit_class_header(name) {
    return ("public static class Codex_" + (sanitize(name) + "\n{\n"));
}

function emit_defs(defs, i) {
    return ((i === BigInt(defs.length)) ? "" : (emit_def(defs[Number(i)]) + ("\n" + emit_defs(defs, (i + 1n)))));
}

function lower_bin_op(op, ty) {
    return ((_s) => (_s.tag === "OpAdd" ? IrAddInt() : (_s.tag === "OpSub" ? IrSubInt() : (_s.tag === "OpMul" ? IrMulInt() : (_s.tag === "OpDiv" ? IrDivInt() : (_s.tag === "OpPow" ? IrPowInt() : (_s.tag === "OpEq" ? IrEq() : (_s.tag === "OpNotEq" ? IrNotEq() : (_s.tag === "OpLt" ? IrLt() : (_s.tag === "OpGt" ? IrGt() : (_s.tag === "OpLtEq" ? IrLtEq() : (_s.tag === "OpGtEq" ? IrGtEq() : (_s.tag === "OpDefEq" ? IrEq() : (_s.tag === "OpAppend" ? IrAppendList() : (_s.tag === "OpCons" ? IrConsList() : (_s.tag === "OpAnd" ? IrAnd() : (_s.tag === "OpOr" ? IrOr() : (() => { throw new Error("Non-exhaustive match"); })())))))))))))))))))(op);
}

function lower_expr(e, ty) {
    return ((_s) => (_s.tag === "ALitExpr" ? (() => { const text = _s.field0; const kind = _s.field1; return lower_literal(text, kind); })() : (_s.tag === "ANameExpr" ? (() => { const name = _s.field0; return IrName(name.value, ty); })() : (_s.tag === "AApplyExpr" ? (() => { const f = _s.field0; const a = _s.field1; return lower_apply(f, a, ty); })() : (_s.tag === "ABinaryExpr" ? (() => { const l = _s.field0; const op = _s.field1; const r = _s.field2; return IrBinary(lower_bin_op(op, ty), lower_expr(l, ty), lower_expr(r, ty), ty); })() : (_s.tag === "AUnaryExpr" ? (() => { const operand = _s.field0; return IrNegate(lower_expr(operand, IntegerTy())); })() : (_s.tag === "AIfExpr" ? (() => { const c = _s.field0; const t = _s.field1; const e2 = _s.field2; return IrIf(lower_expr(c, BooleanTy()), lower_expr(t, ty), lower_expr(e2, ty), ty); })() : (_s.tag === "ALetExpr" ? (() => { const binds = _s.field0; const body = _s.field1; return lower_let(binds, body, ty); })() : (_s.tag === "ALambdaExpr" ? (() => { const params = _s.field0; const body = _s.field1; return lower_lambda(params, body, ty); })() : (_s.tag === "AMatchExpr" ? (() => { const scrut = _s.field0; const arms = _s.field1; return lower_match(scrut, arms, ty); })() : (_s.tag === "AListExpr" ? (() => { const elems = _s.field0; return lower_list(elems, ty); })() : (_s.tag === "ARecordExpr" ? (() => { const name = _s.field0; const fields = _s.field1; return lower_record(name, fields, ty); })() : (_s.tag === "AFieldAccess" ? (() => { const rec = _s.field0; const field = _s.field1; return IrFieldAccess(lower_expr(rec, ty), field.value, ty); })() : (_s.tag === "ADoExpr" ? (() => { const stmts = _s.field0; return lower_do(stmts, ty); })() : (_s.tag === "AErrorExpr" ? (() => { const msg = _s.field0; return IrError(msg, ty); })() : (() => { throw new Error("Non-exhaustive match"); })())))))))))))))))(e);
}

function lower_literal(text, kind) {
    return ((_s) => (_s.tag === "IntLit" ? IrIntLit(parseInt(text, 10)) : (_s.tag === "NumLit" ? IrIntLit(parseInt(text, 10)) : (_s.tag === "TextLit" ? IrTextLit(text) : (_s.tag === "BoolLit" ? IrBoolLit((text === "True")) : (() => { throw new Error("Non-exhaustive match"); })())))))(kind);
}

function lower_apply(f, a, ty) {
    return IrApply(lower_expr(f, ty), lower_expr(a, ty), ty);
}

function lower_let(binds, body, ty) {
    return ((BigInt(binds.length) === 0n) ? lower_expr(body, ty) : ((b) => IrLet(b.name.value, ty, lower_expr(b.value, ty), lower_let_rest(binds, body, ty, 1n)))(binds[Number(0n)]));
}

function lower_let_rest(binds, body, ty, i) {
    return ((i === BigInt(binds.length)) ? lower_expr(body, ty) : ((b) => IrLet(b.name.value, ty, lower_expr(b.value, ty), lower_let_rest(binds, body, ty, (i + 1n))))(binds[Number(i)]));
}

function lower_lambda(params, body, ty) {
    return IrLambda(map_list(lower_param, params), lower_expr(body, ty), ty);
}

function lower_param(n) {
    return IRParam(n.value, ErrorTy());
}

function lower_match(scrut, arms, ty) {
    return IrMatch(lower_expr(scrut, ty), map_list(lower_arm(ty), arms), ty);
}

function lower_arm(ty, arm) {
    return IRBranch(lower_pattern(arm.pattern), lower_expr(arm.body, ty));
}

function lower_pattern(p) {
    return ((_s) => (_s.tag === "AVarPat" ? (() => { const name = _s.field0; return IrVarPat(name.value, ErrorTy()); })() : (_s.tag === "ALitPat" ? (() => { const text = _s.field0; const kind = _s.field1; return IrLitPat(text, ErrorTy()); })() : (_s.tag === "ACtorPat" ? (() => { const name = _s.field0; const subs = _s.field1; return IrCtorPat(name.value, map_list(lower_pattern, subs), ErrorTy()); })() : (_s.tag === "AWildPat" ? IrWildPat() : (() => { throw new Error("Non-exhaustive match"); })())))))(p);
}

function lower_list(elems, ty) {
    return IrList(map_list(lower_elem(ty), elems), ty);
}

function lower_elem(ty, e) {
    return lower_expr(e, ty);
}

function lower_record(name, fields, ty) {
    return IrRecord(name.value, map_list(lower_field_val(ty), fields), ty);
}

function lower_field_val(ty, f) {
    return IRFieldVal(f.name.value, lower_expr(f.value, ty));
}

function lower_do(stmts, ty) {
    return IrDo(map_list(lower_do_stmt(ty), stmts), ty);
}

function lower_do_stmt(ty, s) {
    return ((_s) => (_s.tag === "ADoBindStmt" ? (() => { const name = _s.field0; const val = _s.field1; return IrDoBind(name.value, ty, lower_expr(val, ty)); })() : (_s.tag === "ADoExprStmt" ? (() => { const e = _s.field0; return IrDoExec(lower_expr(e, ty)); })() : (() => { throw new Error("Non-exhaustive match"); })())))(s);
}

function lower_def(d) {
    return IRDef(d.name.value, map_list(lower_def_param, d.params), ErrorTy(), lower_expr(d.body, ErrorTy()));
}

function lower_def_param(p) {
    return IRParam(p.name.value, ErrorTy());
}

function lower_module(m) {
    return IRModule(m.name, map_list(lower_def, m.defs));
}

function compile(source, module_name) {
    return ((tokens) => ((st) => ((doc) => ((ast) => ((ir) => emit_full_module(ir, ast.type_defs))(lower_module(ast)))(desugar_document(doc, module_name)))(parse_document(st)))(make_parse_state(tokens)))(tokenize(source));
}

function compile_checked(source, module_name) {
    return ((tokens) => ((st) => ((doc) => ((ast) => ((resolve_result) => ((BigInt(resolve_result.errors.length) > 0n) ? CompileError(resolve_result.errors) : ((check_result) => ((ir) => CompileOk(emit_full_module(ir, ast.type_defs), check_result))(lower_module(ast)))(check_module(ast))))(resolve_module(ast)))(desugar_document(doc, module_name)))(parse_document(st)))(make_parse_state(tokens)))(tokenize(source));
}

function test_source() {
    return "main = 42";
}

function main() {
    (() => {
        console.log(compile(test_source(), "test"));
    })();
}

function empty_scope() {
    return Scope([]);
}

function scope_has(sc, name) {
    return scope_has_loop(sc.names, name, 0n, BigInt(sc.names.length));
}

function scope_has_loop(names, name, i, len) {
    while (true) {
        if ((i === len)) {
            return false;
        } else {
            if ((names[Number(i)] === name)) {
                return true;
            } else {
                const _tco_0 = names;
                const _tco_1 = name;
                const _tco_2 = (i + 1n);
                const _tco_3 = len;
                names = _tco_0;
                name = _tco_1;
                i = _tco_2;
                len = _tco_3;
                continue;
            }
        }
    }
}

function scope_add(sc, name) {
    return Scope([...[name], ...sc.names]);
}

function builtin_names() {
    return ["show", "negate", "True", "False", "Nothing", "print-line", "read-line", "open-file", "read-all", "close-file", "char-at", "text-length", "substring", "is-letter", "is-digit", "is-whitespace", "text-to-integer", "integer-to-text", "text-replace", "char-code", "code-to-char", "list-length", "list-at", "map", "filter", "fold"];
}

function is_type_name(name) {
    return ((BigInt(name.length) === 0n) ? false : ((/^[a-zA-Z]/.test(name[Number(0n)])) && is_upper_char(name[Number(0n)])));
}

function is_upper_char(c) {
    return ((code) => ((code >= 65n) && (code <= 90n)))(BigInt(c.charCodeAt(0)));
}

function collect_top_level_names(defs, i, len, acc, errs) {
    while (true) {
        if ((i === len)) {
            return CollectResult(acc, errs);
        } else {
            const def = defs[Number(i)];
            const name = def.name.value;
            if (list_contains(acc, name)) {
                const _tco_0 = defs;
                const _tco_1 = (i + 1n);
                const _tco_2 = len;
                const _tco_3 = acc;
                const _tco_4 = [...errs, ...[make_error("CDX3001", ("Duplicate definition: " + name))]];
                defs = _tco_0;
                i = _tco_1;
                len = _tco_2;
                acc = _tco_3;
                errs = _tco_4;
                continue;
            } else {
                const _tco_0 = defs;
                const _tco_1 = (i + 1n);
                const _tco_2 = len;
                const _tco_3 = [...acc, ...[name]];
                const _tco_4 = errs;
                defs = _tco_0;
                i = _tco_1;
                len = _tco_2;
                acc = _tco_3;
                errs = _tco_4;
                continue;
            }
        }
    }
}

function list_contains(xs, name) {
    return list_contains_loop(xs, name, 0n, BigInt(xs.length));
}

function list_contains_loop(xs, name, i, len) {
    while (true) {
        if ((i === len)) {
            return false;
        } else {
            if ((xs[Number(i)] === name)) {
                return true;
            } else {
                const _tco_0 = xs;
                const _tco_1 = name;
                const _tco_2 = (i + 1n);
                const _tco_3 = len;
                xs = _tco_0;
                name = _tco_1;
                i = _tco_2;
                len = _tco_3;
                continue;
            }
        }
    }
}

function collect_ctor_names(type_defs, i, len, type_acc, ctor_acc) {
    while (true) {
        if ((i === len)) {
            return CtorCollectResult(type_acc, ctor_acc);
        } else {
            const td = type_defs[Number(i)];
            return ((_s) => (_s.tag === "AVariantTypeDef" ? (() => { const name = _s.field0; const params = _s.field1; const ctors = _s.field2; return ((new_type_acc) => ((new_ctor_acc) => collect_ctor_names(type_defs, (i + 1n), len, new_type_acc, new_ctor_acc))(collect_variant_ctors(ctors, 0n, BigInt(ctors.length), ctor_acc)))([...type_acc, ...[name.value]]); })() : (_s.tag === "ARecordTypeDef" ? (() => { const name = _s.field0; const params = _s.field1; const fields = _s.field2; return collect_ctor_names(type_defs, (i + 1n), len, [...type_acc, ...[name.value]], ctor_acc); })() : (() => { throw new Error("Non-exhaustive match"); })())))(td);
        }
    }
}

function collect_variant_ctors(ctors, i, len, acc) {
    while (true) {
        if ((i === len)) {
            return acc;
        } else {
            const ctor = ctors[Number(i)];
            const _tco_0 = ctors;
            const _tco_1 = (i + 1n);
            const _tco_2 = len;
            const _tco_3 = [...acc, ...[ctor.name.value]];
            ctors = _tco_0;
            i = _tco_1;
            len = _tco_2;
            acc = _tco_3;
            continue;
        }
    }
}

function build_all_names_scope(top_names, ctor_names, builtins) {
    return ((sc) => ((sc2) => add_names_to_scope(sc2, builtins, 0n, BigInt(builtins.length)))(add_names_to_scope(sc, ctor_names, 0n, BigInt(ctor_names.length))))(add_names_to_scope(empty_scope(), top_names, 0n, BigInt(top_names.length)));
}

function add_names_to_scope(sc, names, i, len) {
    while (true) {
        if ((i === len)) {
            return sc;
        } else {
            const _tco_0 = scope_add(sc, names[Number(i)]);
            const _tco_1 = names;
            const _tco_2 = (i + 1n);
            const _tco_3 = len;
            sc = _tco_0;
            names = _tco_1;
            i = _tco_2;
            len = _tco_3;
            continue;
        }
    }
}

function resolve_expr(sc, expr) {
    while (true) {
        return ((_s) => (_s.tag === "ALitExpr" ? (() => { const val = _s.field0; const kind = _s.field1; return []; })() : (_s.tag === "ANameExpr" ? (() => { const name = _s.field0; return ((scope_has(sc, name.value) || is_type_name(name.value)) ? [] : [make_error("CDX3002", ("Undefined name: " + name.value))]); })() : (_s.tag === "ABinaryExpr" ? (() => { const left = _s.field0; const op = _s.field1; const right = _s.field2; return [...resolve_expr(sc, left), ...resolve_expr(sc, right)]; })() : (_s.tag === "AUnaryExpr" ? (() => { const operand = _s.field0; return resolve_expr(sc, operand); })() : (_s.tag === "AApplyExpr" ? (() => { const func = _s.field0; const arg = _s.field1; return [...resolve_expr(sc, func), ...resolve_expr(sc, arg)]; })() : (_s.tag === "AIfExpr" ? (() => { const cond = _s.field0; const then_e = _s.field1; const else_e = _s.field2; return [...resolve_expr(sc, cond), ...[...resolve_expr(sc, then_e), ...resolve_expr(sc, else_e)]]; })() : (_s.tag === "ALetExpr" ? (() => { const bindings = _s.field0; const body = _s.field1; return resolve_let(sc, bindings, body, 0n, BigInt(bindings.length), []); })() : (_s.tag === "ALambdaExpr" ? (() => { const params = _s.field0; const body = _s.field1; return ((sc2) => resolve_expr(sc2, body))(add_lambda_params(sc, params, 0n, BigInt(params.length))); })() : (_s.tag === "AMatchExpr" ? (() => { const scrutinee = _s.field0; const arms = _s.field1; return [...resolve_expr(sc, scrutinee), ...resolve_match_arms(sc, arms, 0n, BigInt(arms.length), [])]; })() : (_s.tag === "AListExpr" ? (() => { const elems = _s.field0; return resolve_list_elems(sc, elems, 0n, BigInt(elems.length), []); })() : (_s.tag === "ARecordExpr" ? (() => { const name = _s.field0; const fields = _s.field1; return resolve_record_fields(sc, fields, 0n, BigInt(fields.length), []); })() : (_s.tag === "AFieldAccess" ? (() => { const obj = _s.field0; const field = _s.field1; return resolve_expr(sc, obj); })() : (_s.tag === "ADoExpr" ? (() => { const stmts = _s.field0; return resolve_do_stmts(sc, stmts, 0n, BigInt(stmts.length), []); })() : (_s.tag === "AErrorExpr" ? (() => { const msg = _s.field0; return []; })() : (() => { throw new Error("Non-exhaustive match"); })())))))))))))))))(expr);
    }
}

function resolve_let(sc, bindings, body, i, len, errs) {
    while (true) {
        if ((i === len)) {
            return [...errs, ...resolve_expr(sc, body)];
        } else {
            const b = bindings[Number(i)];
            const bind_errs = resolve_expr(sc, b.value);
            const sc2 = scope_add(sc, b.name.value);
            const _tco_0 = sc2;
            const _tco_1 = bindings;
            const _tco_2 = body;
            const _tco_3 = (i + 1n);
            const _tco_4 = len;
            const _tco_5 = [...errs, ...bind_errs];
            sc = _tco_0;
            bindings = _tco_1;
            body = _tco_2;
            i = _tco_3;
            len = _tco_4;
            errs = _tco_5;
            continue;
        }
    }
}

function add_lambda_params(sc, params, i, len) {
    while (true) {
        if ((i === len)) {
            return sc;
        } else {
            const p = params[Number(i)];
            const _tco_0 = scope_add(sc, p.value);
            const _tco_1 = params;
            const _tco_2 = (i + 1n);
            const _tco_3 = len;
            sc = _tco_0;
            params = _tco_1;
            i = _tco_2;
            len = _tco_3;
            continue;
        }
    }
}

function resolve_match_arms(sc, arms, i, len, errs) {
    while (true) {
        if ((i === len)) {
            return errs;
        } else {
            const arm = arms[Number(i)];
            const sc2 = collect_pattern_names(sc, arm.pattern);
            const arm_errs = resolve_expr(sc2, arm.body);
            const _tco_0 = sc;
            const _tco_1 = arms;
            const _tco_2 = (i + 1n);
            const _tco_3 = len;
            const _tco_4 = [...errs, ...arm_errs];
            sc = _tco_0;
            arms = _tco_1;
            i = _tco_2;
            len = _tco_3;
            errs = _tco_4;
            continue;
        }
    }
}

function collect_pattern_names(sc, pat) {
    return ((_s) => (_s.tag === "AVarPat" ? (() => { const name = _s.field0; return scope_add(sc, name.value); })() : (_s.tag === "ACtorPat" ? (() => { const name = _s.field0; const subs = _s.field1; return collect_ctor_pat_names(sc, subs, 0n, BigInt(subs.length)); })() : (_s.tag === "ALitPat" ? (() => { const val = _s.field0; const kind = _s.field1; return sc; })() : (_s.tag === "AWildPat" ? sc : (() => { throw new Error("Non-exhaustive match"); })())))))(pat);
}

function collect_ctor_pat_names(sc, subs, i, len) {
    while (true) {
        if ((i === len)) {
            return sc;
        } else {
            const sub = subs[Number(i)];
            const _tco_0 = collect_pattern_names(sc, sub);
            const _tco_1 = subs;
            const _tco_2 = (i + 1n);
            const _tco_3 = len;
            sc = _tco_0;
            subs = _tco_1;
            i = _tco_2;
            len = _tco_3;
            continue;
        }
    }
}

function resolve_list_elems(sc, elems, i, len, errs) {
    while (true) {
        if ((i === len)) {
            return errs;
        } else {
            const errs2 = resolve_expr(sc, elems[Number(i)]);
            const _tco_0 = sc;
            const _tco_1 = elems;
            const _tco_2 = (i + 1n);
            const _tco_3 = len;
            const _tco_4 = [...errs, ...errs2];
            sc = _tco_0;
            elems = _tco_1;
            i = _tco_2;
            len = _tco_3;
            errs = _tco_4;
            continue;
        }
    }
}

function resolve_record_fields(sc, fields, i, len, errs) {
    while (true) {
        if ((i === len)) {
            return errs;
        } else {
            const f = fields[Number(i)];
            const errs2 = resolve_expr(sc, f.value);
            const _tco_0 = sc;
            const _tco_1 = fields;
            const _tco_2 = (i + 1n);
            const _tco_3 = len;
            const _tco_4 = [...errs, ...errs2];
            sc = _tco_0;
            fields = _tco_1;
            i = _tco_2;
            len = _tco_3;
            errs = _tco_4;
            continue;
        }
    }
}

function resolve_do_stmts(sc, stmts, i, len, errs) {
    while (true) {
        if ((i === len)) {
            return errs;
        } else {
            const stmt = stmts[Number(i)];
            return ((_s) => (_s.tag === "ADoExprStmt" ? (() => { const e = _s.field0; return ((errs2) => resolve_do_stmts(sc, stmts, (i + 1n), len, [...errs, ...errs2]))(resolve_expr(sc, e)); })() : (_s.tag === "ADoBindStmt" ? (() => { const name = _s.field0; const e = _s.field1; return ((errs2) => ((sc2) => resolve_do_stmts(sc2, stmts, (i + 1n), len, [...errs, ...errs2]))(scope_add(sc, name.value)))(resolve_expr(sc, e)); })() : (() => { throw new Error("Non-exhaustive match"); })())))(stmt);
        }
    }
}

function resolve_all_defs(sc, defs, i, len, errs) {
    while (true) {
        if ((i === len)) {
            return errs;
        } else {
            const def = defs[Number(i)];
            const def_scope = add_def_params(sc, def.params, 0n, BigInt(def.params.length));
            const errs2 = resolve_expr(def_scope, def.body);
            const _tco_0 = sc;
            const _tco_1 = defs;
            const _tco_2 = (i + 1n);
            const _tco_3 = len;
            const _tco_4 = [...errs, ...errs2];
            sc = _tco_0;
            defs = _tco_1;
            i = _tco_2;
            len = _tco_3;
            errs = _tco_4;
            continue;
        }
    }
}

function add_def_params(sc, params, i, len) {
    while (true) {
        if ((i === len)) {
            return sc;
        } else {
            const p = params[Number(i)];
            const _tco_0 = scope_add(sc, p.name.value);
            const _tco_1 = params;
            const _tco_2 = (i + 1n);
            const _tco_3 = len;
            sc = _tco_0;
            params = _tco_1;
            i = _tco_2;
            len = _tco_3;
            continue;
        }
    }
}

function resolve_module(mod) {
    return ((top) => ((ctors) => ((sc) => ((expr_errs) => ResolveResult([...top.errors, ...expr_errs], top.names, ctors.type_names, ctors.ctor_names))(resolve_all_defs(sc, mod.defs, 0n, BigInt(mod.defs.length), [])))(build_all_names_scope(top.names, ctors.ctor_names, builtin_names())))(collect_ctor_names(mod.type_defs, 0n, BigInt(mod.type_defs.length), [], [])))(collect_top_level_names(mod.defs, 0n, BigInt(mod.defs.length), [], []));
}

function make_lex_state(src) {
    return LexState(src, 0n, 1n, 1n);
}

function is_at_end(st) {
    return (st.offset >= BigInt(st.source.length));
}

function peek_char(st) {
    return (is_at_end(st) ? "" : st.source[Number(st.offset)]);
}

function advance_char(st) {
    return ((peek_char(st) === "\n") ? LexState(st.source, (st.offset + 1n), (st.line + 1n), 1n) : LexState(st.source, (st.offset + 1n), st.line, (st.column + 1n)));
}

function skip_spaces(st) {
    while (true) {
        if (is_at_end(st)) {
            return st;
        } else {
            if ((peek_char(st) === " ")) {
                const _tco_0 = advance_char(st);
                st = _tco_0;
                continue;
            } else {
                return st;
            }
        }
    }
}

function scan_ident_rest(st) {
    while (true) {
        if (is_at_end(st)) {
            return st;
        } else {
            const ch = peek_char(st);
            if ((/^[a-zA-Z]/.test(ch))) {
                const _tco_0 = advance_char(st);
                st = _tco_0;
                continue;
            } else {
                if ((/^[0-9]/.test(ch))) {
                    const _tco_0 = advance_char(st);
                    st = _tco_0;
                    continue;
                } else {
                    if ((ch === "_")) {
                        const _tco_0 = advance_char(st);
                        st = _tco_0;
                        continue;
                    } else {
                        if ((ch === "-")) {
                            const next = advance_char(st);
                            if (is_at_end(next)) {
                                return st;
                            } else {
                                if ((/^[a-zA-Z]/.test(peek_char(next)))) {
                                    const _tco_0 = next;
                                    st = _tco_0;
                                    continue;
                                } else {
                                    return st;
                                }
                            }
                        } else {
                            return st;
                        }
                    }
                }
            }
        }
    }
}

function scan_digits(st) {
    while (true) {
        if (is_at_end(st)) {
            return st;
        } else {
            const ch = peek_char(st);
            if ((/^[0-9]/.test(ch))) {
                const _tco_0 = advance_char(st);
                st = _tco_0;
                continue;
            } else {
                if ((ch === "_")) {
                    const _tco_0 = advance_char(st);
                    st = _tco_0;
                    continue;
                } else {
                    return st;
                }
            }
        }
    }
}

function scan_string_body(st) {
    while (true) {
        if (is_at_end(st)) {
            return st;
        } else {
            const ch = peek_char(st);
            if ((ch === "\"")) {
                return advance_char(st);
            } else {
                if ((ch === "\n")) {
                    return st;
                } else {
                    if ((ch === "\\")) {
                        const _tco_0 = advance_char(advance_char(st));
                        st = _tco_0;
                        continue;
                    } else {
                        const _tco_0 = advance_char(st);
                        st = _tco_0;
                        continue;
                    }
                }
            }
        }
    }
}

function classify_word(w) {
    return ((w === "let") ? LetKeyword() : ((w === "in") ? InKeyword() : ((w === "if") ? IfKeyword() : ((w === "then") ? ThenKeyword() : ((w === "else") ? ElseKeyword() : ((w === "when") ? WhenKeyword() : ((w === "where") ? WhereKeyword() : ((w === "do") ? DoKeyword() : ((w === "record") ? RecordKeyword() : ((w === "import") ? ImportKeyword() : ((w === "export") ? ExportKeyword() : ((w === "claim") ? ClaimKeyword() : ((w === "proof") ? ProofKeyword() : ((w === "forall") ? ForAllKeyword() : ((w === "exists") ? ThereExistsKeyword() : ((w === "linear") ? LinearKeyword() : ((w === "True") ? TrueKeyword() : ((w === "False") ? FalseKeyword() : ((first_code) => ((first_code >= 65n) ? ((first_code <= 90n) ? TypeIdentifier() : Identifier()) : Identifier()))(BigInt(w[Number(0n)].charCodeAt(0)))))))))))))))))))));
}

function make_token(kind, text, st) {
    return Token(kind, text, st.offset, st.line, st.column);
}

function extract_text(st, start, end_st) {
    return st.source.substring(Number(start), Number(start + (end_st.offset - start)));
}

function scan_token(st) {
    return ((s) => (is_at_end(s) ? LexEnd() : ((ch) => ((ch === "\n") ? LexToken(make_token(Newline(), "\n", s), advance_char(s)) : ((ch === "\"") ? ((start) => ((after) => ((text_len) => LexToken(make_token(TextLiteral(), s.source.substring(Number(start), Number(start + text_len)), s), after))(((after.offset - start) - 1n)))(scan_string_body(advance_char(s))))((s.offset + 1n)) : ((/^[a-zA-Z]/.test(ch)) ? ((start) => ((after) => ((word) => LexToken(make_token(classify_word(word), word, s), after))(extract_text(s, start, after)))(scan_ident_rest(advance_char(s))))(s.offset) : ((ch === "_") ? ((start) => ((after) => ((word) => ((BigInt(word.length) === 1n) ? LexToken(make_token(Underscore(), "_", s), after) : LexToken(make_token(classify_word(word), word, s), after)))(extract_text(s, start, after)))(scan_ident_rest(advance_char(s))))(s.offset) : ((/^[0-9]/.test(ch)) ? ((start) => ((after) => (is_at_end(after) ? LexToken(make_token(IntegerLiteral(), extract_text(s, start, after), s), after) : ((peek_char(after) === ".") ? ((after2) => LexToken(make_token(NumberLiteral(), extract_text(s, start, after2), s), after2))(scan_digits(advance_char(after))) : LexToken(make_token(IntegerLiteral(), extract_text(s, start, after), s), after))))(scan_digits(advance_char(s))))(s.offset) : scan_operator(s)))))))(peek_char(s))))(skip_spaces(st));
}

function scan_operator(s) {
    return ((ch) => ((next) => ((ch === "(") ? LexToken(make_token(LeftParen(), "(", s), next) : ((ch === ")") ? LexToken(make_token(RightParen(), ")", s), next) : ((ch === "[") ? LexToken(make_token(LeftBracket(), "[", s), next) : ((ch === "]") ? LexToken(make_token(RightBracket(), "]", s), next) : ((ch === "{") ? LexToken(make_token(LeftBrace(), "{", s), next) : ((ch === "}") ? LexToken(make_token(RightBrace(), "}", s), next) : ((ch === ",") ? LexToken(make_token(Comma(), ",", s), next) : ((ch === ".") ? LexToken(make_token(Dot(), ".", s), next) : ((ch === "^") ? LexToken(make_token(Caret(), "^", s), next) : ((ch === "&") ? LexToken(make_token(Ampersand(), "&", s), next) : scan_multi_char_operator(s))))))))))))(advance_char(s)))(peek_char(s));
}

function scan_multi_char_operator(s) {
    return ((ch) => ((next) => ((next_ch) => ((ch === "+") ? ((next_ch === "+") ? LexToken(make_token(PlusPlus(), "++", s), advance_char(next)) : LexToken(make_token(Plus(), "+", s), next)) : ((ch === "-") ? ((next_ch === ">") ? LexToken(make_token(Arrow(), "->", s), advance_char(next)) : LexToken(make_token(Minus(), "-", s), next)) : ((ch === "*") ? LexToken(make_token(Star(), "*", s), next) : ((ch === "/") ? ((next_ch === "=") ? LexToken(make_token(NotEquals(), "/=", s), advance_char(next)) : LexToken(make_token(Slash(), "/", s), next)) : ((ch === "=") ? ((next_ch === "=") ? ((next2) => ((next2_ch) => ((next2_ch === "=") ? LexToken(make_token(TripleEquals(), "===", s), advance_char(next2)) : LexToken(make_token(DoubleEquals(), "==", s), next2)))((is_at_end(next2) ? "" : peek_char(next2))))(advance_char(next)) : LexToken(make_token(Equals(), "=", s), next)) : ((ch === ":") ? ((next_ch === ":") ? LexToken(make_token(ColonColon(), "::", s), advance_char(next)) : LexToken(make_token(Colon(), ":", s), next)) : ((ch === "|") ? ((next_ch === "-") ? LexToken(make_token(Turnstile(), "|-", s), advance_char(next)) : LexToken(make_token(Pipe(), "|", s), next)) : ((ch === "<") ? ((next_ch === "=") ? LexToken(make_token(LessOrEqual(), "<=", s), advance_char(next)) : ((next_ch === "-") ? LexToken(make_token(LeftArrow(), "<-", s), advance_char(next)) : LexToken(make_token(LessThan(), "<", s), next))) : ((ch === ">") ? ((next_ch === "=") ? LexToken(make_token(GreaterOrEqual(), ">=", s), advance_char(next)) : LexToken(make_token(GreaterThan(), ">", s), next)) : LexToken(make_token(ErrorToken(), s.source[Number(s.offset)], s), next)))))))))))((is_at_end(next) ? "" : peek_char(next))))(advance_char(s)))(peek_char(s));
}

function tokenize_loop(st, acc) {
    while (true) {
        return ((_s) => (_s.tag === "LexToken" ? (() => { const tok = _s.field0; const next = _s.field1; return ((tok.kind === EndOfFile()) ? [...acc, ...[tok]] : tokenize_loop(next, [...acc, ...[tok]])); })() : (_s.tag === "LexEnd" ? [...acc, ...[make_token(EndOfFile(), "", st)]] : (() => { throw new Error("Non-exhaustive match"); })())))(scan_token(st));
    }
}

function tokenize(src) {
    return tokenize_loop(make_lex_state(src), []);
}

function make_parse_state(toks) {
    return ParseState(toks, 0n);
}

function current(st) {
    return st.tokens[Number(st.pos)];
}

function current_kind(st) {
    return current(st).kind;
}

function advance(st) {
    return ParseState(st.tokens, (st.pos + 1n));
}

function is_done(st) {
    return ((_s) => (_s.tag === "EndOfFile" ? true : ((_) => false)(_s)))(current_kind(st));
}

function peek_kind(st, offset) {
    return st.tokens[Number((st.pos + offset))].kind;
}

function is_ident(k) {
    return ((_s) => (_s.tag === "Identifier" ? true : ((_) => false)(_s)))(k);
}

function is_type_ident(k) {
    return ((_s) => (_s.tag === "TypeIdentifier" ? true : ((_) => false)(_s)))(k);
}

function is_arrow(k) {
    return ((_s) => (_s.tag === "Arrow" ? true : ((_) => false)(_s)))(k);
}

function is_equals(k) {
    return ((_s) => (_s.tag === "Equals" ? true : ((_) => false)(_s)))(k);
}

function is_colon(k) {
    return ((_s) => (_s.tag === "Colon" ? true : ((_) => false)(_s)))(k);
}

function is_comma(k) {
    return ((_s) => (_s.tag === "Comma" ? true : ((_) => false)(_s)))(k);
}

function is_pipe(k) {
    return ((_s) => (_s.tag === "Pipe" ? true : ((_) => false)(_s)))(k);
}

function is_dot(k) {
    return ((_s) => (_s.tag === "Dot" ? true : ((_) => false)(_s)))(k);
}

function is_left_paren(k) {
    return ((_s) => (_s.tag === "LeftParen" ? true : ((_) => false)(_s)))(k);
}

function is_left_brace(k) {
    return ((_s) => (_s.tag === "LeftBrace" ? true : ((_) => false)(_s)))(k);
}

function is_left_bracket(k) {
    return ((_s) => (_s.tag === "LeftBracket" ? true : ((_) => false)(_s)))(k);
}

function is_right_brace(k) {
    return ((_s) => (_s.tag === "RightBrace" ? true : ((_) => false)(_s)))(k);
}

function is_right_bracket(k) {
    return ((_s) => (_s.tag === "RightBracket" ? true : ((_) => false)(_s)))(k);
}

function is_if_keyword(k) {
    return ((_s) => (_s.tag === "IfKeyword" ? true : ((_) => false)(_s)))(k);
}

function is_let_keyword(k) {
    return ((_s) => (_s.tag === "LetKeyword" ? true : ((_) => false)(_s)))(k);
}

function is_when_keyword(k) {
    return ((_s) => (_s.tag === "WhenKeyword" ? true : ((_) => false)(_s)))(k);
}

function is_do_keyword(k) {
    return ((_s) => (_s.tag === "DoKeyword" ? true : ((_) => false)(_s)))(k);
}

function is_in_keyword(k) {
    return ((_s) => (_s.tag === "InKeyword" ? true : ((_) => false)(_s)))(k);
}

function is_minus(k) {
    return ((_s) => (_s.tag === "Minus" ? true : ((_) => false)(_s)))(k);
}

function is_dedent(k) {
    return ((_s) => (_s.tag === "Dedent" ? true : ((_) => false)(_s)))(k);
}

function is_left_arrow(k) {
    return ((_s) => (_s.tag === "LeftArrow" ? true : ((_) => false)(_s)))(k);
}

function is_record_keyword(k) {
    return ((_s) => (_s.tag === "RecordKeyword" ? true : ((_) => false)(_s)))(k);
}

function is_underscore(k) {
    return ((_s) => (_s.tag === "Underscore" ? true : ((_) => false)(_s)))(k);
}

function is_literal(k) {
    return ((_s) => (_s.tag === "IntegerLiteral" ? true : (_s.tag === "NumberLiteral" ? true : (_s.tag === "TextLiteral" ? true : (_s.tag === "TrueKeyword" ? true : (_s.tag === "FalseKeyword" ? true : ((_) => false)(_s)))))))(k);
}

function is_app_start(k) {
    return ((_s) => (_s.tag === "Identifier" ? true : (_s.tag === "TypeIdentifier" ? true : (_s.tag === "IntegerLiteral" ? true : (_s.tag === "NumberLiteral" ? true : (_s.tag === "TextLiteral" ? true : (_s.tag === "TrueKeyword" ? true : (_s.tag === "FalseKeyword" ? true : (_s.tag === "LeftParen" ? true : (_s.tag === "LeftBracket" ? true : ((_) => false)(_s)))))))))))(k);
}

function is_compound(e) {
    return ((_s) => (_s.tag === "MatchExpr" ? (() => { const s = _s.field0; const arms = _s.field1; return true; })() : (_s.tag === "IfExpr" ? (() => { const c = _s.field0; const t = _s.field1; const el = _s.field2; return true; })() : (_s.tag === "LetExpr" ? (() => { const binds = _s.field0; const body = _s.field1; return true; })() : (_s.tag === "DoExpr" ? (() => { const stmts = _s.field0; return true; })() : ((_) => false)(_s))))))(e);
}

function is_type_arg_start(k) {
    return ((_s) => (_s.tag === "TypeIdentifier" ? true : (_s.tag === "Identifier" ? true : (_s.tag === "LeftParen" ? true : ((_) => false)(_s)))))(k);
}

function operator_precedence(k) {
    return ((_s) => (_s.tag === "PlusPlus" ? 5n : (_s.tag === "ColonColon" ? 5n : (_s.tag === "Plus" ? 6n : (_s.tag === "Minus" ? 6n : (_s.tag === "Star" ? 7n : (_s.tag === "Slash" ? 7n : (_s.tag === "Caret" ? 8n : (_s.tag === "DoubleEquals" ? 4n : (_s.tag === "NotEquals" ? 4n : (_s.tag === "LessThan" ? 4n : (_s.tag === "GreaterThan" ? 4n : (_s.tag === "LessOrEqual" ? 4n : (_s.tag === "GreaterOrEqual" ? 4n : (_s.tag === "TripleEquals" ? 4n : (_s.tag === "Ampersand" ? 3n : (_s.tag === "Pipe" ? 2n : ((_) => (0n - 1n))(_s))))))))))))))))))(k);
}

function expect(kind, st) {
    return (is_done(st) ? st : advance(st));
}

function skip_newlines(st) {
    while (true) {
        if (is_done(st)) {
            return st;
        } else {
            return ((_s) => (_s.tag === "Newline" ? skip_newlines(advance(st)) : (_s.tag === "Indent" ? skip_newlines(advance(st)) : (_s.tag === "Dedent" ? skip_newlines(advance(st)) : ((_) => st)(_s)))))(current_kind(st));
        }
    }
}

function parse_type(st) {
    return ((result) => unwrap_type_ok(result, parse_type_continue))(parse_type_atom(st));
}

function parse_type_continue(left, st) {
    return (is_arrow(current_kind(st)) ? ((st2) => ((right_result) => unwrap_type_ok(right_result, make_fun_type(left)))(parse_type(st2)))(advance(st)) : TypeOk(left, st));
}

function make_fun_type(left, right, st) {
    return TypeOk(FunType(left, right), st);
}

function unwrap_type_ok(r, f) {
    return ((_s) => (_s.tag === "TypeOk" ? (() => { const t = _s.field0; const st = _s.field1; return f(t)(st); })() : (() => { throw new Error("Non-exhaustive match"); })()))(r);
}

function parse_type_atom(st) {
    return (is_ident(current_kind(st)) ? ((tok) => parse_type_args(NamedType(tok), advance(st)))(current(st)) : (is_type_ident(current_kind(st)) ? ((tok) => parse_type_args(NamedType(tok), advance(st)))(current(st)) : (is_left_paren(current_kind(st)) ? parse_paren_type(advance(st)) : ((tok) => TypeOk(NamedType(tok), advance(st)))(current(st)))));
}

function parse_paren_type(st) {
    return ((inner) => unwrap_type_ok(inner, finish_paren_type))(parse_type(st));
}

function finish_paren_type(t, st) {
    return ((st2) => TypeOk(ParenType(t), st2))(expect(RightParen(), st));
}

function parse_type_args(base_type, st) {
    return (is_done(st) ? TypeOk(base_type, st) : (is_type_arg_start(current_kind(st)) ? parse_type_arg_next(base_type, st) : TypeOk(base_type, st)));
}

function parse_type_arg_next(base_type, st) {
    return ((arg_result) => unwrap_type_ok(arg_result, continue_type_args(base_type)))(parse_type_atom(st));
}

function continue_type_args(base_type, arg, st) {
    return parse_type_args(AppType(base_type, [arg]), st);
}

function parse_pattern(st) {
    return (is_underscore(current_kind(st)) ? PatOk(WildPat(current(st)), advance(st)) : (is_literal(current_kind(st)) ? PatOk(LitPat(current(st)), advance(st)) : (is_type_ident(current_kind(st)) ? ((tok) => parse_ctor_pattern_fields(tok, [], advance(st)))(current(st)) : (is_ident(current_kind(st)) ? PatOk(VarPat(current(st)), advance(st)) : PatOk(WildPat(current(st)), advance(st))))));
}

function parse_ctor_pattern_fields(ctor, acc, st) {
    return (is_left_paren(current_kind(st)) ? ((st2) => ((sub) => unwrap_pat_ok(sub, continue_ctor_fields(ctor)(acc)))(parse_pattern(st2)))(advance(st)) : PatOk(CtorPat(ctor, acc), st));
}

function continue_ctor_fields(ctor, acc, p, st) {
    return ((st2) => parse_ctor_pattern_fields(ctor, [...acc, ...[p]], st2))(expect(RightParen(), st));
}

function unwrap_pat_ok(r, f) {
    return ((_s) => (_s.tag === "PatOk" ? (() => { const p = _s.field0; const st = _s.field1; return f(p)(st); })() : (() => { throw new Error("Non-exhaustive match"); })()))(r);
}

function parse_expr(st) {
    return parse_binary(st, 0n);
}

function unwrap_expr_ok(r, f) {
    return ((_s) => (_s.tag === "ExprOk" ? (() => { const e = _s.field0; const st = _s.field1; return f(e)(st); })() : (() => { throw new Error("Non-exhaustive match"); })()))(r);
}

function parse_binary(st, min_prec) {
    return ((left_result) => unwrap_expr_ok(left_result, start_binary_loop(min_prec)))(parse_unary(st));
}

function start_binary_loop(min_prec, left, st) {
    return parse_binary_loop(left, st, min_prec);
}

function parse_binary_loop(left, st, min_prec) {
    return (is_done(st) ? ExprOk(left, st) : ((prec) => ((prec < min_prec) ? ExprOk(left, st) : ((op) => ((st2) => ((right_result) => unwrap_expr_ok(right_result, continue_binary(left)(op)(min_prec)))(parse_binary(st2, (prec + 1n))))(skip_newlines(advance(st))))(current(st))))(operator_precedence(current_kind(st))));
}

function continue_binary(left, op, min_prec, right, st) {
    return parse_binary_loop(BinExpr(left, op, right), st, min_prec);
}

function parse_unary(st) {
    return (is_minus(current_kind(st)) ? ((op) => ((result) => unwrap_expr_ok(result, finish_unary(op)))(parse_unary(advance(st))))(current(st)) : parse_application(st));
}

function finish_unary(op, operand, st) {
    return ExprOk(UnaryExpr(op, operand), st);
}

function parse_application(st) {
    return ((func_result) => unwrap_expr_ok(func_result, parse_app_loop))(parse_atom(st));
}

function parse_app_loop(func, st) {
    return (is_compound(func) ? parse_field_access(func, st) : (is_done(st) ? ExprOk(func, st) : (is_app_start(current_kind(st)) ? ((arg_result) => unwrap_expr_ok(arg_result, continue_app(func)))(parse_atom(st)) : parse_field_access(func, st))));
}

function continue_app(func, arg, st) {
    return parse_app_loop(AppExpr(func, arg), st);
}

function parse_atom(st) {
    return (is_literal(current_kind(st)) ? ExprOk(LitExpr(current(st)), advance(st)) : (is_ident(current_kind(st)) ? parse_field_access(NameExpr(current(st)), advance(st)) : (is_type_ident(current_kind(st)) ? parse_atom_type_ident(st) : (is_left_paren(current_kind(st)) ? parse_paren_expr(advance(st)) : (is_left_bracket(current_kind(st)) ? parse_list_expr(st) : (is_if_keyword(current_kind(st)) ? parse_if_expr(st) : (is_let_keyword(current_kind(st)) ? parse_let_expr(st) : (is_when_keyword(current_kind(st)) ? parse_match_expr(st) : (is_do_keyword(current_kind(st)) ? parse_do_expr(st) : ExprOk(ErrExpr(current(st)), advance(st)))))))))));
}

function parse_field_access(node, st) {
    while (true) {
        if (is_dot(current_kind(st))) {
            const st2 = advance(st);
            const field = current(st2);
            const st3 = advance(st2);
            const _tco_0 = FieldExpr(node, field);
            const _tco_1 = st3;
            node = _tco_0;
            st = _tco_1;
            continue;
        } else {
            return ExprOk(node, st);
        }
    }
}

function parse_atom_type_ident(st) {
    return ((tok) => ((st2) => (is_left_brace(current_kind(st2)) ? parse_record_expr(tok, st2) : ExprOk(NameExpr(tok), st2)))(advance(st)))(current(st));
}

function parse_paren_expr(st) {
    return ((st2) => ((inner) => unwrap_expr_ok(inner, finish_paren_expr))(parse_expr(st2)))(skip_newlines(st));
}

function finish_paren_expr(e, st) {
    return ((st2) => ((st3) => ExprOk(ParenExpr(e), st3))(expect(RightParen(), st2)))(skip_newlines(st));
}

function parse_record_expr(type_name, st) {
    return ((st2) => ((st3) => parse_record_expr_fields(type_name, [], st3))(skip_newlines(st2)))(advance(st));
}

function parse_record_expr_fields(type_name, acc, st) {
    return (is_right_brace(current_kind(st)) ? ExprOk(RecordExpr(type_name, acc), advance(st)) : (is_ident(current_kind(st)) ? parse_record_field(type_name, acc, st) : ExprOk(RecordExpr(type_name, acc), st)));
}

function parse_record_field(type_name, acc, st) {
    return ((field_name) => ((st2) => ((st3) => ((val_result) => unwrap_expr_ok(val_result, finish_record_field(type_name)(acc)(field_name)))(parse_expr(st3)))(expect(Equals(), st2)))(advance(st)))(current(st));
}

function finish_record_field(type_name, acc, field_name, v, st) {
    return ((field) => ((st2) => (is_comma(current_kind(st2)) ? parse_record_expr_fields(type_name, [...acc, ...[field]], skip_newlines(advance(st2))) : parse_record_expr_fields(type_name, [...acc, ...[field]], st2)))(skip_newlines(st)))(RecordFieldExpr(field_name, v));
}

function parse_list_expr(st) {
    return ((st2) => ((st3) => parse_list_elements([], st3))(skip_newlines(st2)))(advance(st));
}

function parse_list_elements(acc, st) {
    return (is_right_bracket(current_kind(st)) ? ExprOk(ListExpr(acc), advance(st)) : ((elem) => unwrap_expr_ok(elem, finish_list_element(acc)))(parse_expr(st)));
}

function finish_list_element(acc, e, st) {
    return ((st2) => (is_comma(current_kind(st2)) ? parse_list_elements([...acc, ...[e]], skip_newlines(advance(st2))) : parse_list_elements([...acc, ...[e]], st2)))(skip_newlines(st));
}

function parse_if_expr(st) {
    return ((st2) => ((cond) => unwrap_expr_ok(cond, parse_if_then))(parse_expr(st2)))(skip_newlines(advance(st)));
}

function parse_if_then(c, st) {
    return ((st2) => ((st3) => ((st4) => ((then_result) => unwrap_expr_ok(then_result, parse_if_else(c)))(parse_expr(st4)))(skip_newlines(st3)))(expect(ThenKeyword(), st2)))(skip_newlines(st));
}

function parse_if_else(c, t, st) {
    return ((st2) => ((st3) => ((st4) => ((else_result) => unwrap_expr_ok(else_result, finish_if(c)(t)))(parse_expr(st4)))(skip_newlines(st3)))(expect(ElseKeyword(), st2)))(skip_newlines(st));
}

function finish_if(c, t, e, st) {
    return ExprOk(IfExpr(c, t, e), st);
}

function parse_let_expr(st) {
    return ((st2) => parse_let_bindings([], st2))(skip_newlines(advance(st)));
}

function parse_let_bindings(acc, st) {
    return (is_ident(current_kind(st)) ? parse_let_binding(acc, st) : (is_in_keyword(current_kind(st)) ? ((st2) => ((body) => unwrap_expr_ok(body, finish_let(acc)))(parse_expr(st2)))(skip_newlines(advance(st))) : ((body) => unwrap_expr_ok(body, finish_let(acc)))(parse_expr(st))));
}

function finish_let(acc, b, st) {
    return ExprOk(LetExpr(acc, b), st);
}

function parse_let_binding(acc, st) {
    return ((name_tok) => ((st2) => ((st3) => ((val_result) => unwrap_expr_ok(val_result, finish_let_binding(acc)(name_tok)))(parse_expr(st3)))(expect(Equals(), st2)))(advance(st)))(current(st));
}

function finish_let_binding(acc, name_tok, v, st) {
    return ((binding) => ((st2) => (is_comma(current_kind(st2)) ? parse_let_bindings([...acc, ...[binding]], skip_newlines(advance(st2))) : parse_let_bindings([...acc, ...[binding]], st2)))(skip_newlines(st)))(LetBind(name_tok, v));
}

function parse_match_expr(st) {
    return ((st2) => ((scrut) => unwrap_expr_ok(scrut, start_match_branches))(parse_expr(st2)))(advance(st));
}

function start_match_branches(s, st) {
    return ((st2) => parse_match_branches(s, [], st2))(skip_newlines(st));
}

function parse_match_branches(scrut, acc, st) {
    return (is_if_keyword(current_kind(st)) ? parse_one_match_branch(scrut, acc, st) : ExprOk(MatchExpr(scrut, acc), st));
}

function unwrap_pat_for_expr(r, f) {
    return ((_s) => (_s.tag === "PatOk" ? (() => { const p = _s.field0; const st = _s.field1; return f(p)(st); })() : (() => { throw new Error("Non-exhaustive match"); })()))(r);
}

function parse_one_match_branch(scrut, acc, st) {
    return ((st2) => ((pat) => unwrap_pat_for_expr(pat, parse_match_branch_body(scrut)(acc)))(parse_pattern(st2)))(advance(st));
}

function parse_match_branch_body(scrut, acc, p, st) {
    return ((st2) => ((st3) => ((body) => unwrap_expr_ok(body, finish_match_branch(scrut)(acc)(p)))(parse_expr(st3)))(skip_newlines(st2)))(expect(Arrow(), st));
}

function finish_match_branch(scrut, acc, p, b, st) {
    return ((arm) => ((st2) => parse_match_branches(scrut, [...acc, ...[arm]], st2))(skip_newlines(st)))(MatchArm(p, b));
}

function parse_do_expr(st) {
    return ((st2) => parse_do_stmts([], st2))(skip_newlines(advance(st)));
}

function parse_do_stmts(acc, st) {
    return (is_done(st) ? ExprOk(DoExpr(acc), st) : (is_dedent(current_kind(st)) ? ExprOk(DoExpr(acc), st) : (is_do_bind(st) ? parse_do_bind_stmt(acc, st) : parse_do_expr_stmt(acc, st))));
}

function is_do_bind(st) {
    return (is_ident(current_kind(st)) ? is_left_arrow(peek_kind(st, 1n)) : false);
}

function parse_do_bind_stmt(acc, st) {
    return ((name_tok) => ((st2) => ((val_result) => unwrap_expr_ok(val_result, finish_do_bind(acc)(name_tok)))(parse_expr(st2)))(advance(advance(st))))(current(st));
}

function finish_do_bind(acc, name_tok, v, st) {
    return ((st2) => parse_do_stmts([...acc, ...[DoBindStmt(name_tok, v)]], st2))(skip_newlines(st));
}

function parse_do_expr_stmt(acc, st) {
    return ((expr_result) => unwrap_expr_ok(expr_result, finish_do_expr(acc)))(parse_expr(st));
}

function finish_do_expr(acc, e, st) {
    return ((st2) => parse_do_stmts([...acc, ...[DoExprStmt(e)]], st2))(skip_newlines(st));
}

function parse_type_annotation(st) {
    return ((st2) => ((st3) => parse_type(st3))(expect(Colon(), st2)))(advance(st));
}

function parse_definition(st) {
    return (is_done(st) ? DefNone(st) : (is_ident(current_kind(st)) ? try_parse_def(st) : (is_type_ident(current_kind(st)) ? try_parse_def(st) : DefNone(st))));
}

function try_parse_def(st) {
    return (is_colon(peek_kind(st, 1n)) ? ((ann_result) => unwrap_type_for_def(ann_result))(parse_type_annotation(st)) : parse_def_body_with_ann([], st));
}

function unwrap_type_for_def(r) {
    return ((_s) => (_s.tag === "TypeOk" ? (() => { const ann_type = _s.field0; const st = _s.field1; return ((name_tok) => ((ann) => ((st2) => parse_def_body_with_ann(ann, st2))(skip_newlines(st)))([TypeAnn(name_tok, ann_type)]))(Token(Identifier(), "", 0n, 0n, 0n)); })() : (() => { throw new Error("Non-exhaustive match"); })()))(r);
}

function parse_def_body_with_ann(ann, st) {
    return ((name_tok) => ((st2) => parse_def_params_then(ann, name_tok, [], st2))(advance(st)))(current(st));
}

function parse_def_params_then(ann, name_tok, acc, st) {
    while (true) {
        if (is_left_paren(current_kind(st))) {
            const st2 = advance(st);
            if (is_ident(current_kind(st2))) {
                const param = current(st2);
                const st3 = advance(st2);
                const st4 = expect(RightParen(), st3);
                const _tco_0 = ann;
                const _tco_1 = name_tok;
                const _tco_2 = [...acc, ...[param]];
                const _tco_3 = st4;
                ann = _tco_0;
                name_tok = _tco_1;
                acc = _tco_2;
                st = _tco_3;
                continue;
            } else {
                return finish_def(ann, name_tok, acc, st);
            }
        } else {
            return finish_def(ann, name_tok, acc, st);
        }
    }
}

function finish_def(ann, name_tok, params, st) {
    return ((st2) => ((st3) => ((body_result) => unwrap_def_body(body_result, ann, name_tok, params))(parse_expr(st3)))(skip_newlines(st2)))(expect(Equals(), st));
}

function unwrap_def_body(r, ann, name_tok, params) {
    return ((_s) => (_s.tag === "ExprOk" ? (() => { const b = _s.field0; const st = _s.field1; return DefOk(Def(name_tok, params, ann, b), st); })() : (() => { throw new Error("Non-exhaustive match"); })()))(r);
}

function parse_type_def(st) {
    return (is_type_ident(current_kind(st)) ? ((name_tok) => ((st2) => (is_equals(current_kind(st2)) ? ((st3) => (is_record_keyword(current_kind(st3)) ? parse_record_type(name_tok, st3) : (is_pipe(current_kind(st3)) ? parse_variant_type(name_tok, st3) : TypeDefNone(st))))(skip_newlines(advance(st2))) : TypeDefNone(st)))(advance(st)))(current(st)) : TypeDefNone(st));
}

function parse_record_type(name_tok, st) {
    return ((st2) => ((st3) => ((st4) => parse_record_fields_loop(name_tok, [], st4))(skip_newlines(st3)))(expect(LeftBrace(), st2)))(advance(st));
}

function parse_record_fields_loop(name_tok, acc, st) {
    return (is_right_brace(current_kind(st)) ? TypeDefOk(TypeDef(name_tok, [], RecordBody(acc)), advance(st)) : (is_ident(current_kind(st)) ? parse_one_record_field(name_tok, acc, st) : TypeDefOk(TypeDef(name_tok, [], RecordBody(acc)), st)));
}

function parse_one_record_field(name_tok, acc, st) {
    return ((field_name) => ((st2) => ((st3) => ((field_type_result) => unwrap_record_field_type(name_tok, acc, field_name, field_type_result))(parse_type(st3)))(expect(Colon(), st2)))(advance(st)))(current(st));
}

function unwrap_record_field_type(name_tok, acc, field_name, r) {
    return ((_s) => (_s.tag === "TypeOk" ? (() => { const ft = _s.field0; const st = _s.field1; return ((field) => ((st2) => (is_comma(current_kind(st2)) ? parse_record_fields_loop(name_tok, [...acc, ...[field]], skip_newlines(advance(st2))) : parse_record_fields_loop(name_tok, [...acc, ...[field]], st2)))(skip_newlines(st)))(RecordFieldDef(field_name, ft)); })() : (() => { throw new Error("Non-exhaustive match"); })()))(r);
}

function parse_variant_type(name_tok, st) {
    return parse_variant_ctors(name_tok, [], st);
}

function parse_variant_ctors(name_tok, acc, st) {
    return (is_pipe(current_kind(st)) ? ((st2) => ((ctor_name) => ((st3) => parse_ctor_fields(ctor_name, [], st3, name_tok, acc))(advance(st2)))(current(st2)))(skip_newlines(advance(st))) : TypeDefOk(TypeDef(name_tok, [], VariantBody(acc)), st));
}

function parse_ctor_fields(ctor_name, fields, st, name_tok, acc) {
    return (is_left_paren(current_kind(st)) ? ((field_result) => unwrap_ctor_field(field_result, ctor_name, fields, name_tok, acc))(parse_type(advance(st))) : ((st2) => ((ctor) => parse_variant_ctors(name_tok, [...acc, ...[ctor]], st2))(VariantCtorDef(ctor_name, fields)))(skip_newlines(st)));
}

function unwrap_ctor_field(r, ctor_name, fields, name_tok, acc) {
    return ((_s) => (_s.tag === "TypeOk" ? (() => { const ty = _s.field0; const st = _s.field1; return ((st2) => parse_ctor_fields(ctor_name, [...fields, ...[ty]], st2, name_tok, acc))(expect(RightParen(), st)); })() : (() => { throw new Error("Non-exhaustive match"); })()))(r);
}

function parse_document(st) {
    return ((st2) => parse_top_level([], [], st2))(skip_newlines(st));
}

function parse_top_level(defs, type_defs, st) {
    return (is_done(st) ? Document(defs, type_defs) : try_top_level_type_def(defs, type_defs, st));
}

function try_top_level_type_def(defs, type_defs, st) {
    return ((td_result) => ((_s) => (_s.tag === "TypeDefOk" ? (() => { const td = _s.field0; const st2 = _s.field1; return parse_top_level(defs, [...type_defs, ...[td]], skip_newlines(st2)); })() : (_s.tag === "TypeDefNone" ? (() => { const st2 = _s.field0; return try_top_level_def(defs, type_defs, st); })() : (() => { throw new Error("Non-exhaustive match"); })())))(td_result))(parse_type_def(st));
}

function try_top_level_def(defs, type_defs, st) {
    return ((def_result) => ((_s) => (_s.tag === "DefOk" ? (() => { const d = _s.field0; const st2 = _s.field1; return parse_top_level([...defs, ...[d]], type_defs, skip_newlines(st2)); })() : (_s.tag === "DefNone" ? (() => { const st2 = _s.field0; return parse_top_level(defs, type_defs, skip_newlines(advance(st2))); })() : (() => { throw new Error("Non-exhaustive match"); })())))(def_result))(parse_definition(st));
}

function token_length(t) {
    return BigInt(t.text.length);
}

function infer_literal(st, kind) {
    return ((_s) => (_s.tag === "IntLit" ? CheckResult(IntegerTy(), st) : (_s.tag === "NumLit" ? CheckResult(NumberTy(), st) : (_s.tag === "TextLit" ? CheckResult(TextTy(), st) : (_s.tag === "BoolLit" ? CheckResult(BooleanTy(), st) : (() => { throw new Error("Non-exhaustive match"); })())))))(kind);
}

function infer_name(st, env, name) {
    return (env_has(env, name) ? CheckResult(env_lookup(env, name), st) : CheckResult(ErrorTy(), add_unify_error(st, "CDX2002", ("Unknown name: " + name))));
}

function infer_binary(st, env, left, op, right) {
    return ((lr) => ((rr) => infer_binary_op(rr.state, lr.inferred_type, rr.inferred_type, op))(infer_expr(lr.state, env, right)))(infer_expr(st, env, left));
}

function infer_binary_op(st, lt, rt, op) {
    return ((_s) => (_s.tag === "OpAdd" ? infer_arithmetic(st, lt, rt) : (_s.tag === "OpSub" ? infer_arithmetic(st, lt, rt) : (_s.tag === "OpMul" ? infer_arithmetic(st, lt, rt) : (_s.tag === "OpDiv" ? infer_arithmetic(st, lt, rt) : (_s.tag === "OpPow" ? infer_arithmetic(st, lt, rt) : (_s.tag === "OpEq" ? infer_comparison(st, lt, rt) : (_s.tag === "OpNotEq" ? infer_comparison(st, lt, rt) : (_s.tag === "OpLt" ? infer_comparison(st, lt, rt) : (_s.tag === "OpGt" ? infer_comparison(st, lt, rt) : (_s.tag === "OpLtEq" ? infer_comparison(st, lt, rt) : (_s.tag === "OpGtEq" ? infer_comparison(st, lt, rt) : (_s.tag === "OpAnd" ? infer_logical(st, lt, rt) : (_s.tag === "OpOr" ? infer_logical(st, lt, rt) : (_s.tag === "OpAppend" ? infer_append(st, lt, rt) : (_s.tag === "OpCons" ? infer_cons(st, lt, rt) : (_s.tag === "OpDefEq" ? infer_comparison(st, lt, rt) : (() => { throw new Error("Non-exhaustive match"); })())))))))))))))))))(op);
}

function infer_arithmetic(st, lt, rt) {
    return ((r) => CheckResult(lt, r.state))(unify(st, lt, rt));
}

function infer_comparison(st, lt, rt) {
    return ((r) => CheckResult(BooleanTy(), r.state))(unify(st, lt, rt));
}

function infer_logical(st, lt, rt) {
    return ((r1) => ((r2) => CheckResult(BooleanTy(), r2.state))(unify(r1.state, rt, BooleanTy())))(unify(st, lt, BooleanTy()));
}

function infer_append(st, lt, rt) {
    return ((resolved) => ((_s) => (_s.tag === "TextTy" ? ((r) => CheckResult(TextTy(), r.state))(unify(st, rt, TextTy())) : ((_) => ((r) => CheckResult(lt, r.state))(unify(st, lt, rt)))(_s)))(resolved))(resolve(st, lt));
}

function infer_cons(st, lt, rt) {
    return ((list_ty) => ((r) => CheckResult(list_ty, r.state))(unify(st, rt, list_ty)))(ListTy(lt));
}

function infer_if(st, env, cond, then_e, else_e) {
    return ((cr) => ((r1) => ((tr) => ((er) => ((r2) => CheckResult(tr.inferred_type, r2.state))(unify(er.state, tr.inferred_type, er.inferred_type)))(infer_expr(tr.state, env, else_e)))(infer_expr(r1.state, env, then_e)))(unify(cr.state, cr.inferred_type, BooleanTy())))(infer_expr(st, env, cond));
}

function infer_let(st, env, bindings, body) {
    return ((env2) => infer_expr(env2.state, env2.env, body))(infer_let_bindings(st, env, bindings, 0n, BigInt(bindings.length)));
}

function infer_let_bindings(st, env, bindings, i, len) {
    while (true) {
        if ((i === len)) {
            return LetBindResult(st, env);
        } else {
            const b = bindings[Number(i)];
            const vr = infer_expr(st, env, b.value);
            const env2 = env_bind(env, b.name.value, vr.inferred_type);
            const _tco_0 = vr.state;
            const _tco_1 = env2;
            const _tco_2 = bindings;
            const _tco_3 = (i + 1n);
            const _tco_4 = len;
            st = _tco_0;
            env = _tco_1;
            bindings = _tco_2;
            i = _tco_3;
            len = _tco_4;
            continue;
        }
    }
}

function infer_lambda(st, env, params, body) {
    return ((pr) => ((br) => ((fun_ty) => CheckResult(fun_ty, br.state))(wrap_fun_type(pr.param_types, br.inferred_type)))(infer_expr(pr.state, pr.env, body)))(bind_lambda_params(st, env, params, 0n, BigInt(params.length), []));
}

function bind_lambda_params(st, env, params, i, len, acc) {
    while (true) {
        if ((i === len)) {
            return LambdaBindResult(st, env, acc);
        } else {
            const p = params[Number(i)];
            const fr = fresh_and_advance(st);
            const env2 = env_bind(env, p.value, fr.var_type);
            const _tco_0 = fr.state;
            const _tco_1 = env2;
            const _tco_2 = params;
            const _tco_3 = (i + 1n);
            const _tco_4 = len;
            const _tco_5 = [...acc, ...[fr.var_type]];
            st = _tco_0;
            env = _tco_1;
            params = _tco_2;
            i = _tco_3;
            len = _tco_4;
            acc = _tco_5;
            continue;
        }
    }
}

function wrap_fun_type(param_types, result) {
    return wrap_fun_type_loop(param_types, result, (BigInt(param_types.length) - 1n));
}

function wrap_fun_type_loop(param_types, result, i) {
    while (true) {
        if ((i < 0n)) {
            return result;
        } else {
            const _tco_0 = param_types;
            const _tco_1 = FunTy(param_types[Number(i)], result);
            const _tco_2 = (i - 1n);
            param_types = _tco_0;
            result = _tco_1;
            i = _tco_2;
            continue;
        }
    }
}

function infer_application(st, env, func, arg) {
    return ((fr) => ((ar) => ((ret) => ((r) => CheckResult(ret.var_type, r.state))(unify(ret.state, fr.inferred_type, FunTy(ar.inferred_type, ret.var_type))))(fresh_and_advance(ar.state)))(infer_expr(fr.state, env, arg)))(infer_expr(st, env, func));
}

function infer_list(st, env, elems) {
    return ((BigInt(elems.length) === 0n) ? ((fr) => CheckResult(ListTy(fr.var_type), fr.state))(fresh_and_advance(st)) : ((first) => ((st2) => CheckResult(ListTy(first.inferred_type), st2))(unify_list_elems(first.state, env, elems, first.inferred_type, 1n, BigInt(elems.length))))(infer_expr(st, env, elems[Number(0n)])));
}

function unify_list_elems(st, env, elems, elem_ty, i, len) {
    while (true) {
        if ((i === len)) {
            return st;
        } else {
            const er = infer_expr(st, env, elems[Number(i)]);
            const r = unify(er.state, er.inferred_type, elem_ty);
            const _tco_0 = r.state;
            const _tco_1 = env;
            const _tco_2 = elems;
            const _tco_3 = elem_ty;
            const _tco_4 = (i + 1n);
            const _tco_5 = len;
            st = _tco_0;
            env = _tco_1;
            elems = _tco_2;
            elem_ty = _tco_3;
            i = _tco_4;
            len = _tco_5;
            continue;
        }
    }
}

function infer_match(st, env, scrutinee, arms) {
    return ((sr) => ((fr) => ((st2) => CheckResult(fr.var_type, st2))(infer_match_arms(fr.state, env, sr.inferred_type, fr.var_type, arms, 0n, BigInt(arms.length))))(fresh_and_advance(sr.state)))(infer_expr(st, env, scrutinee));
}

function infer_match_arms(st, env, scrut_ty, result_ty, arms, i, len) {
    while (true) {
        if ((i === len)) {
            return st;
        } else {
            const arm = arms[Number(i)];
            const env2 = bind_pattern(env, arm.pattern, scrut_ty);
            const br = infer_expr(st, env2, arm.body);
            const r = unify(br.state, br.inferred_type, result_ty);
            const _tco_0 = r.state;
            const _tco_1 = env;
            const _tco_2 = scrut_ty;
            const _tco_3 = result_ty;
            const _tco_4 = arms;
            const _tco_5 = (i + 1n);
            const _tco_6 = len;
            st = _tco_0;
            env = _tco_1;
            scrut_ty = _tco_2;
            result_ty = _tco_3;
            arms = _tco_4;
            i = _tco_5;
            len = _tco_6;
            continue;
        }
    }
}

function bind_pattern(env, pat, ty) {
    return ((_s) => (_s.tag === "AVarPat" ? (() => { const name = _s.field0; return env_bind(env, name.value, ty); })() : (_s.tag === "AWildPat" ? env : ((_) => env)(_s))))(pat);
}

function infer_do(st, env, stmts) {
    return infer_do_loop(st, env, stmts, 0n, BigInt(stmts.length), NothingTy());
}

function infer_do_loop(st, env, stmts, i, len, last_ty) {
    while (true) {
        if ((i === len)) {
            return CheckResult(last_ty, st);
        } else {
            const stmt = stmts[Number(i)];
            return ((_s) => (_s.tag === "ADoExprStmt" ? (() => { const e = _s.field0; return ((er) => infer_do_loop(er.state, env, stmts, (i + 1n), len, er.inferred_type))(infer_expr(st, env, e)); })() : (_s.tag === "ADoBindStmt" ? (() => { const name = _s.field0; const e = _s.field1; return ((er) => ((env2) => infer_do_loop(er.state, env2, stmts, (i + 1n), len, er.inferred_type))(env_bind(env, name.value, er.inferred_type)))(infer_expr(st, env, e)); })() : (() => { throw new Error("Non-exhaustive match"); })())))(stmt);
        }
    }
}

function infer_expr(st, env, expr) {
    return ((_s) => (_s.tag === "ALitExpr" ? (() => { const val = _s.field0; const kind = _s.field1; return infer_literal(st, kind); })() : (_s.tag === "ANameExpr" ? (() => { const name = _s.field0; return infer_name(st, env, name.value); })() : (_s.tag === "ABinaryExpr" ? (() => { const left = _s.field0; const op = _s.field1; const right = _s.field2; return infer_binary(st, env, left, op, right); })() : (_s.tag === "AUnaryExpr" ? (() => { const operand = _s.field0; return ((r) => ((u) => CheckResult(IntegerTy(), u.state))(unify(r.state, r.inferred_type, IntegerTy())))(infer_expr(st, env, operand)); })() : (_s.tag === "AApplyExpr" ? (() => { const func = _s.field0; const arg = _s.field1; return infer_application(st, env, func, arg); })() : (_s.tag === "AIfExpr" ? (() => { const cond = _s.field0; const then_e = _s.field1; const else_e = _s.field2; return infer_if(st, env, cond, then_e, else_e); })() : (_s.tag === "ALetExpr" ? (() => { const bindings = _s.field0; const body = _s.field1; return infer_let(st, env, bindings, body); })() : (_s.tag === "ALambdaExpr" ? (() => { const params = _s.field0; const body = _s.field1; return infer_lambda(st, env, params, body); })() : (_s.tag === "AMatchExpr" ? (() => { const scrutinee = _s.field0; const arms = _s.field1; return infer_match(st, env, scrutinee, arms); })() : (_s.tag === "AListExpr" ? (() => { const elems = _s.field0; return infer_list(st, env, elems); })() : (_s.tag === "ADoExpr" ? (() => { const stmts = _s.field0; return infer_do(st, env, stmts); })() : (_s.tag === "AFieldAccess" ? (() => { const obj = _s.field0; const field = _s.field1; return ((r) => CheckResult(ErrorTy(), r.state))(infer_expr(st, env, obj)); })() : (_s.tag === "ARecordExpr" ? (() => { const name = _s.field0; const fields = _s.field1; return CheckResult(ErrorTy(), st); })() : (_s.tag === "AErrorExpr" ? (() => { const msg = _s.field0; return CheckResult(ErrorTy(), st); })() : (() => { throw new Error("Non-exhaustive match"); })())))))))))))))))(expr);
}

function resolve_type_expr(texpr) {
    while (true) {
        return ((_s) => (_s.tag === "ANamedType" ? (() => { const name = _s.field0; return resolve_type_name(name.value); })() : (_s.tag === "AFunType" ? (() => { const param = _s.field0; const ret = _s.field1; return FunTy(resolve_type_expr(param), resolve_type_expr(ret)); })() : (_s.tag === "AAppType" ? (() => { const ctor = _s.field0; const args = _s.field1; return resolve_type_expr(ctor); })() : (() => { throw new Error("Non-exhaustive match"); })()))))(texpr);
    }
}

function resolve_type_name(name) {
    return ((name === "Integer") ? IntegerTy() : ((name === "Number") ? NumberTy() : ((name === "Text") ? TextTy() : ((name === "Boolean") ? BooleanTy() : ((name === "Nothing") ? NothingTy() : ConstructedTy(Name(name), []))))));
}

function check_def(st, env, def) {
    return ((declared) => ((env2) => ((body_r) => ((u) => CheckResult(declared.expected_type, u.state))(unify(body_r.state, env2.remaining_type, body_r.inferred_type)))(infer_expr(env2.state, env2.env, def.body)))(bind_def_params(declared.state, declared.env, def.params, declared.expected_type, 0n, BigInt(def.params.length))))(resolve_declared_type(st, def));
}

function resolve_declared_type(st, def) {
    return ((BigInt(def.declared_type.length) === 0n) ? ((fr) => DefSetup(fr.var_type, fr.var_type, fr.state, builtin_type_env()))(fresh_and_advance(st)) : ((ty) => DefSetup(ty, ty, st, builtin_type_env()))(resolve_type_expr(def.declared_type[Number(0n)])));
}

function bind_def_params(st, env, params, remaining, i, len) {
    while (true) {
        if ((i === len)) {
            return DefParamResult(st, env, remaining);
        } else {
            const p = params[Number(i)];
            return ((_s) => (_s.tag === "FunTy" ? (() => { const param_ty = _s.field0; const ret_ty = _s.field1; return ((env2) => bind_def_params(st, env2, params, ret_ty, (i + 1n), len))(env_bind(env, p.name.value, param_ty)); })() : ((_) => ((fr) => ((env2) => bind_def_params(fr.state, env2, params, remaining, (i + 1n), len))(env_bind(env, p.name.value, fr.var_type)))(fresh_and_advance(st)))(_s)))(remaining);
        }
    }
}

function check_module(mod) {
    return ((env) => check_all_defs(env.state, env.env, mod.defs, 0n, BigInt(mod.defs.length), []))(register_all_defs(empty_unification_state(), builtin_type_env(), mod.defs, 0n, BigInt(mod.defs.length)));
}

function register_all_defs(st, env, defs, i, len) {
    while (true) {
        if ((i === len)) {
            return LetBindResult(st, env);
        } else {
            const def = defs[Number(i)];
            const ty = ((BigInt(def.declared_type.length) === 0n) ? ((fr) => ((env2) => LetBindResult(fr.state, env2))(env_bind(env, def.name.value, fr.var_type)))(fresh_and_advance(st)) : ((resolved) => LetBindResult(st, env_bind(env, def.name.value, resolved)))(resolve_type_expr(def.declared_type[Number(0n)])));
            const _tco_0 = ty.state;
            const _tco_1 = ty.env;
            const _tco_2 = defs;
            const _tco_3 = (i + 1n);
            const _tco_4 = len;
            st = _tco_0;
            env = _tco_1;
            defs = _tco_2;
            i = _tco_3;
            len = _tco_4;
            continue;
        }
    }
}

function check_all_defs(st, env, defs, i, len, acc) {
    while (true) {
        if ((i === len)) {
            return ModuleResult(acc, st);
        } else {
            const def = defs[Number(i)];
            const r = check_def(st, env, def);
            const resolved = deep_resolve(r.state, r.inferred_type);
            const entry = TypeBinding(def.name.value, resolved);
            const _tco_0 = r.state;
            const _tco_1 = env;
            const _tco_2 = defs;
            const _tco_3 = (i + 1n);
            const _tco_4 = len;
            const _tco_5 = [...acc, ...[entry]];
            st = _tco_0;
            env = _tco_1;
            defs = _tco_2;
            i = _tco_3;
            len = _tco_4;
            acc = _tco_5;
            continue;
        }
    }
}

function empty_type_env() {
    return TypeEnv([]);
}

function env_lookup(env, name) {
    return env_lookup_loop(env.bindings, name, 0n, BigInt(env.bindings.length));
}

function env_lookup_loop(bindings, name, i, len) {
    while (true) {
        if ((i === len)) {
            return ErrorTy();
        } else {
            const b = bindings[Number(i)];
            if ((b.name === name)) {
                return b.bound_type;
            } else {
                const _tco_0 = bindings;
                const _tco_1 = name;
                const _tco_2 = (i + 1n);
                const _tco_3 = len;
                bindings = _tco_0;
                name = _tco_1;
                i = _tco_2;
                len = _tco_3;
                continue;
            }
        }
    }
}

function env_has(env, name) {
    return env_has_loop(env.bindings, name, 0n, BigInt(env.bindings.length));
}

function env_has_loop(bindings, name, i, len) {
    while (true) {
        if ((i === len)) {
            return false;
        } else {
            const b = bindings[Number(i)];
            if ((b.name === name)) {
                return true;
            } else {
                const _tco_0 = bindings;
                const _tco_1 = name;
                const _tco_2 = (i + 1n);
                const _tco_3 = len;
                bindings = _tco_0;
                name = _tco_1;
                i = _tco_2;
                len = _tco_3;
                continue;
            }
        }
    }
}

function env_bind(env, name, ty) {
    return TypeEnv([...[TypeBinding(name, ty)], ...env.bindings]);
}

function builtin_type_env() {
    return ((e) => ((e2) => ((e3) => ((e4) => ((e5) => ((e6) => ((e7) => ((e8) => ((e9) => ((e10) => ((e11) => ((e12) => ((e13) => e13)(env_bind(e12, "text-to-integer", FunTy(TextTy(), IntegerTy()))))(env_bind(e11, "text-replace", FunTy(TextTy(), FunTy(TextTy(), FunTy(TextTy(), TextTy()))))))(env_bind(e10, "code-to-char", FunTy(IntegerTy(), TextTy()))))(env_bind(e9, "char-code", FunTy(TextTy(), IntegerTy()))))(env_bind(e8, "is-whitespace", FunTy(TextTy(), BooleanTy()))))(env_bind(e7, "is-digit", FunTy(TextTy(), BooleanTy()))))(env_bind(e6, "is-letter", FunTy(TextTy(), BooleanTy()))))(env_bind(e5, "substring", FunTy(TextTy(), FunTy(IntegerTy(), FunTy(IntegerTy(), TextTy()))))))(env_bind(e4, "char-at", FunTy(TextTy(), FunTy(IntegerTy(), TextTy())))))(env_bind(e3, "integer-to-text", FunTy(IntegerTy(), TextTy()))))(env_bind(e2, "text-length", FunTy(TextTy(), IntegerTy()))))(env_bind(e, "negate", FunTy(IntegerTy(), IntegerTy()))))(empty_type_env());
}

function empty_unification_state() {
    return UnificationState([], 0n, []);
}

function fresh_var(st) {
    return TypeVar(st.next_id);
}

function advance_id(st) {
    return UnificationState(st.substitutions, (st.next_id + 1n), st.errors);
}

function fresh_and_advance(st) {
    return FreshResult(TypeVar(st.next_id), advance_id(st));
}

function subst_lookup(var_id, entries) {
    return subst_lookup_loop(var_id, entries, 0n, BigInt(entries.length));
}

function subst_lookup_loop(var_id, entries, i, len) {
    while (true) {
        if ((i === len)) {
            return ErrorTy();
        } else {
            const entry = entries[Number(i)];
            if ((entry.var_id === var_id)) {
                return entry.resolved_type;
            } else {
                const _tco_0 = var_id;
                const _tco_1 = entries;
                const _tco_2 = (i + 1n);
                const _tco_3 = len;
                var_id = _tco_0;
                entries = _tco_1;
                i = _tco_2;
                len = _tco_3;
                continue;
            }
        }
    }
}

function has_subst(var_id, entries) {
    return has_subst_loop(var_id, entries, 0n, BigInt(entries.length));
}

function has_subst_loop(var_id, entries, i, len) {
    while (true) {
        if ((i === len)) {
            return false;
        } else {
            const entry = entries[Number(i)];
            if ((entry.var_id === var_id)) {
                return true;
            } else {
                const _tco_0 = var_id;
                const _tco_1 = entries;
                const _tco_2 = (i + 1n);
                const _tco_3 = len;
                var_id = _tco_0;
                entries = _tco_1;
                i = _tco_2;
                len = _tco_3;
                continue;
            }
        }
    }
}

function resolve(st, ty) {
    while (true) {
        return ((_s) => (_s.tag === "TypeVar" ? (() => { const id = _s.field0; return (has_subst(id, st.substitutions) ? resolve(st, subst_lookup(id, st.substitutions)) : ty); })() : ((_) => ty)(_s)))(ty);
    }
}

function add_subst(st, var_id, ty) {
    return UnificationState([...st.substitutions, ...[SubstEntry(var_id, ty)]], st.next_id, st.errors);
}

function add_unify_error(st, code, msg) {
    return UnificationState(st.substitutions, st.next_id, [...st.errors, ...[make_error(code, msg)]]);
}

function occurs_in(st, var_id, ty) {
    while (true) {
        const resolved = resolve(st, ty);
        return ((_s) => (_s.tag === "TypeVar" ? (() => { const id = _s.field0; return (id === var_id); })() : (_s.tag === "FunTy" ? (() => { const param = _s.field0; const ret = _s.field1; return (occurs_in(st, var_id, param) || occurs_in(st, var_id, ret)); })() : (_s.tag === "ListTy" ? (() => { const elem = _s.field0; return occurs_in(st, var_id, elem); })() : ((_) => false)(_s)))))(resolved);
    }
}

function unify(st, a, b) {
    return ((ra) => ((rb) => unify_resolved(st, ra, rb))(resolve(st, b)))(resolve(st, a));
}

function unify_resolved(st, a, b) {
    return ((_s) => (_s.tag === "TypeVar" ? (() => { const id_a = _s.field0; return (occurs_in(st, id_a, b) ? UnifyResult(false, add_unify_error(st, "CDX2010", "Infinite type")) : UnifyResult(true, add_subst(st, id_a, b))); })() : ((_) => unify_rhs(st, a, b))(_s)))(a);
}

function unify_rhs(st, a, b) {
    return ((_s) => (_s.tag === "TypeVar" ? (() => { const id_b = _s.field0; return (occurs_in(st, id_b, a) ? UnifyResult(false, add_unify_error(st, "CDX2010", "Infinite type")) : UnifyResult(true, add_subst(st, id_b, a))); })() : ((_) => unify_structural(st, a, b))(_s)))(b);
}

function unify_structural(st, a, b) {
    return ((_s) => (_s.tag === "IntegerTy" ? ((_s) => (_s.tag === "IntegerTy" ? UnifyResult(true, st) : ((_) => unify_mismatch(st, a, b))(_s)))(b) : (() => { throw new Error("Non-exhaustive match"); })()))(a);
}

function unify_fun(st, pa, ra, pb, rb) {
    return ((r1) => (r1.success ? unify(r1.state, ra, rb) : r1))(unify(st, pa, pb));
}

function unify_mismatch(st, a, b) {
    return UnifyResult(false, add_unify_error(st, "CDX2001", "Type mismatch"));
}

function deep_resolve(st, ty) {
    return ((resolved) => ((_s) => (_s.tag === "FunTy" ? (() => { const param = _s.field0; const ret = _s.field1; return FunTy(deep_resolve(st, param), deep_resolve(st, ret)); })() : (_s.tag === "ListTy" ? (() => { const elem = _s.field0; return ListTy(deep_resolve(st, elem)); })() : ((_) => resolved)(_s))))(resolved))(resolve(st, ty));
}

main();
