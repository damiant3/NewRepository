id=escape-copy-bare-metal
title=Enable escape-copy heap reclamation on bare metal
status=active
updated=2026-03-30
agent=cam
summary=Bare-metal escape-copy skip removed and tested. Mechanism is code-compatible (no syscalls) but ~880x too slow: 11KB/180s vs 193KB/60s. Root cause: per-region overhead (fwd table zero + graph traversal) with thousands of small regions. Needs architectural fix — see handoff.
