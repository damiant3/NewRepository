square : Integer -> Integer
square (x) = x * x

greet : Text -> Text
greet (name) = "Hello, " ++ name ++ "!"

main : [Console] Nothing
main = do
  print-line (greet "Codex")
  print-line (show (square 7))
