Chapter: Person

Section: Definitions

  Person = record {
    name : Text,
    age : Integer
  }

  greet : Person -> Text
  greet (p) = "Hello, " ++ p.name ++ "!"

  main : Text = greet (Person { name = "Alice", age = 30 })

Page 1
