using System;
using System.Collections.Generic;
using System.Linq;

public static class Codex_DepLib
{
    public static long @double(long x)
    {
        return (x + x);
    }

    public static long triple(long x)
    {
        return ((x + x) + x);
    }

}
