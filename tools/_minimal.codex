MyType =
 | ListTy (Text)
 | ErrorTy

get-type : Integer -> MyType
get-type (x) = ListTy "hello"

f : Text -> Integer -> Text
f (n) (args) =
 if n == "a" then let et = when get-type args if ListTy (t) -> t if _ -> "err" in et
 else "unknown"

main : Integer
main = text-length (f "a" 42)
